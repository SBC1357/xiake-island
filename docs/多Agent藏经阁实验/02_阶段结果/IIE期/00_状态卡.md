# IIE期状态卡

状态：Terminated Early / Evidence Preserved  
日期：2026-04-04  
用途：记录 `IIE期` 提前终止后的正式结果侧状态，明确哪些内容继续保留为证据、哪些后续动作已经转入 `IIF`。

## 证据摘要

- 已直接核对 [03_批次A_首标结果_旧口径重写版.md](C0_主中心/03_批次A_首标结果_旧口径重写版.md)、[04_批次B_首标结果.md](C0_主中心/04_批次B_首标结果.md)、[C1_结构复核中心/02_批次B_抽样盲复核结果.md](C1_结构复核中心/02_批次B_抽样盲复核结果.md) 与 [C2_领域复核中心/02_批次B_抽样盲复核结果.md](C2_领域复核中心/02_批次B_抽样盲复核结果.md)，确认 `IIE` 已完成 `32` 篇样本的首标与高风险抽样复核，结果完整可读。
- 已直接核对 [藏经阁条件性对象表_20260403.md](../../00_实验中心/藏经阁条件性对象表_20260403.md)，确认当前正式表内仍同时存在 `条件性对象 / 稿型子型` 两类说法，且尚未做“最小单元 vs 有限组合”总审计。
- 当前线程已明确确认：`IIE` 暴露出的重大隐患，不是旧骨架被直接打穿，而是“`IIE` 之前发现过的说法里，可能混入大量非最小单元的有限组合”；若继续在 `IIE` 内压边界，容易把组合态误冻成正式规则。

## 当前阶段口径

- 当前正式结果阶段：`IIE期（已提前终止）`
- 当前一句话口径：`IIE` 的现有结果全部保留为证据，但不再继续形成“`IIE` 全量汇总”或继续做桥接前压缩；当前主线已转入 `IIF` 的“最小单元审计 + 32篇复验”
- 当前中心机制：`IIE` 期内已完成的 `1主2复核` 结果保留，不再继续扩期

## 当前结果落盘情况

- `C0_主中心`
  - 已启动
  - 原批次 A `6槽位` 版首标已撤回
  - 旧口径重写版已完成并入结果区
  - 批次 B `24` 篇首标已完成并入结果区
- `C1_结构复核中心`
  - 批次 A 盲复核已完成
- `C2_领域复核中心`
  - 批次 A 盲复核已完成
  - 批次 B 抽样盲复核已完成（8篇高风险样本）
- `IIE期` 根目录
  - [01_批次A三路分歧对照表.md](01_批次A三路分歧对照表.md) 已落盘
  - [02_批次A规则边界待压缩清单.md](02_批次A规则边界待压缩清单.md) 已落盘

## 当前结果区与过渡区分工

进入结果区的内容：

1. 已启动的正式阶段状态
2. 各中心正式提交物
3. 各批次结果记录和首标 / 复核结果

继续留在过渡区的内容：

1. 强留痕
2. 过渡讨论
3. 尚未冻结的阶段口径
4. 仍属于补充执行性质的设计文档
5. [03_IIE桥接层需求苗头清单.md](../../03_阶段过渡/IIE-IIF中间环节/03_补充执行文档.md) 这类面向下一阶段桥接设计的过渡文档

## 当前下一步

1. `IIE` 结果整体作为 `IIF` 审计输入保留，不再继续扩样、继续压边界或继续桥接。
2. 正式进入 `IIF`：先对 `IIE` 之前所有发现过的说法做最小单元审计，再用这 `32` 篇样本复验。
