# 02_批次A_首标结果

状态：Withdrawn（待按旧口径重写）  
日期：2026-04-04  
用途：说明本文件原以本轮临场引入的 `6槽位` 口径组织，现已撤回，不再作为 `IIE期/C0_主中心` 的当前有效首标结果引用。

## 证据摘要

- 已直接核对 [藏经阁IIB期实验方案_20260403.md](../../../01_实验方案/藏经阁IIB期实验方案_20260403.md)，确认前序稳定单篇问法是 `7` 个固定问题，而不是 `6槽位`。
- 已直接核对 [14_IIE实验执行草案.md](../../../03_阶段过渡/IIE-IIF中间环节/03_补充执行文档.md)，确认 `IIE` 现行口径已回退到 `6槽位` 诞生前的既有骨架口径。
- 已直接核对当前文件原内容确实按 `6槽位` 组织，因此不再符合现行口径。

## 当前处理结论

1. 原 `6槽位` 版批次 A `C0` 首标结果全部撤回
2. 本文件不再作为 `IIE期` 当前有效结果引用
3. 批次 A 的样本清单、源文入口和中心分工仍然有效
4. 如需继续推进，必须按回退后的旧口径重写

## 保留的有效部分

下面这些信息仍然有效，可在重写时继续使用：

1. [01_批次A_校准批执行记录.md](01_批次A_校准批执行记录.md) 中冻结的 `8` 篇样本
2. 每篇源文与 supplement 样本的路径
3. 已完成的段落编号方法 `p01, p02...`

## 待办

1. 按 [14_IIE实验执行草案.md](../../../03_阶段过渡/IIE-IIF中间环节/03_补充执行文档.md) 的回退口径，重写批次 A 的 `C0` 首标
2. 在重写完成前，`C1/C2` 不应继续基于原 `6槽位` 版内容做比对
