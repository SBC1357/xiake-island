# IIF期状态卡

状态：Terminated Early / Evidence Preserved  
日期：2026-04-05  
用途：记录 `IIF期` 提前终止后的正式结果侧状态，明确哪些内容继续保留为证据、哪些后续动作已转入 `IIF-I期中间环节`

## 证据摘要

- 已直接核对 [IIE期/00_状态卡.md](../IIE期/00_状态卡.md)，确认 `IIE` 已提前终止，现有结果整体只作为 `IIF` 的审计与复验输入保留。
- 已直接核对 [藏经阁IIF期实验方案_20260404.md](../../01_实验方案/藏经阁IIF期实验方案_20260404.md)，确认 `IIF` 的唯一正式目标是：先做“前置说法最小单元审计”，再对 `IIE` 的 `32` 篇样本做复验。
- 已直接核对 [C0_审计整编中心/02_C1C2交叉裁决表.md](C0_审计整编中心/02_C1C2交叉裁决表.md)，确认 `42` 条主审计清单已完成交叉裁决，当前已形成 `21` 条可单条进入复验的最小单元、`14` 条只能按组合处理的条目、`7` 条观察项。
- 已直接核对 [IIE-IIF中间环节/02_讨论结果与阶段决议.md](../../03_阶段过渡/IIE-IIF中间环节/02_讨论结果与阶段决议.md)，确认 `IIF` 原设计是不先宣判 `II期` 整体失败，而是先跑完 `32` 篇复验。
- 当前线程已明确确认：围绕 `IIF` 复验输入继续向前讨论后，当前主问题已从“在旧 `II期` 口径内继续复验”转成“以 `8` 条公式重整写作规范层对象体系”；若继续在 `IIF` 内直接启动 `32` 篇复验，容易再次把混层说法误冻成正式口径。

## 当前阶段口径

- 当前正式结果阶段：`IIF期（已提前终止）`
- 当前一句话口径：`IIF` 已完成前置说法最小单元审计与交叉裁决，但在 `32` 篇复验启动前提前终止；现有结果整体保留为证据，后续转入 [IIF-I期中间环节](../../03_阶段过渡/IIF-I期中间环节) 讨论“是否重回 `I期` 重整范围内资产”

## 当前正式复验输入

- `21` 条最小单元基底：对应 [C0_审计整编中心/02_C1C2交叉裁决表.md](C0_审计整编中心/02_C1C2交叉裁决表.md) 中的 `复验主单元表`，继续保留为 `IIF` 结果证据。
- `14` 条组合处理映射：对应同表中的 `组合处理映射表`，继续保留为 `IIF` 审计结果，不在本期继续推进成 `32` 篇复验执行输入。
- `7` 条观察残留：对应同表中的 `观察项备忘`，继续保留为审计证据。

## 当前组织方式

- `总协调 / 实验中心`
  - 负责阶段口径、状态同步、目录治理与末端裁决
- `C0_审计整编中心`
  - 负责整编前置说法并完成 `32` 篇复验汇总
- `C1_结构裁判中心`
  - 负责判断最小单元 / 有限组合 / 旧容器
- `C2_功能裁判中心`
  - 负责判断写作目的候选 / 条件性对象候选 / 稿型子型候选 / 旧母路线容器 / 暂不能判

## 当前固定结果路径

正式结果统一落在：

`.`

## 当前下一步

1. `IIF` 结果区整体保留为证据，不再在本期内直接启动 `32` 篇复验。
2. 当前阶段讨论转入 [IIF-I期中间环节](../../03_阶段过渡/IIF-I期中间环节)，先判断是否重回 `I期` 而不是继续启动 `IIG`。
3. `II期` 是否整体失败、是否整体改线，不再由 `IIF` 结果区单独裁决，而由新过渡目录统一收口。


