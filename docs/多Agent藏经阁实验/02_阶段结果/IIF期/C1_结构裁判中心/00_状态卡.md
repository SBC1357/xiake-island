# C1_结构裁判中心 / 00_状态卡

状态：Active  
日期：2026-04-05  
角色：C1_结构裁判中心  
用途：记录 `C1_结构裁判中心` 在 `IIF` 期对 `42` 条主审计清单所做的结构裁判状态、冻结口径和结果摘要

## 证据摘要

- 已直接核对 [藏经阁IIF期实验方案_20260404.md](../../../01_实验方案/藏经阁IIF期实验方案_20260404.md)，确认当前正式阶段为 `IIF 最小单元审计与32篇复验期`，且 `C1` 只负责结构裁判，不做功能归属判断。
- 已直接核对 [IIF期/00_状态卡.md](../00_状态卡.md) 与 [C0_审计整编中心/00_状态卡.md](../C0_审计整编中心/00_状态卡.md)，确认本轮主裁判范围仅为 `42` 条主审计清单，`17` 条 `IIE` 参考附录不并列主裁判。
- 已直接核对 [C0_审计整编中心/01_前置说法整编与审计底稿.md](../C0_审计整编中心/01_前置说法整编与审计底稿.md)，确认主审计清单覆盖 `ACT-01~ACT-08`、`CMP-01~CMP-06`、`RUL-01~RUL-06`、`CO-01~CO-07`、`GS-01~GS-06`、`ARCH-01~ARCH-06`、`OBS-01~OBS-03`。

## 当前阶段口径

- 当前正式阶段：`IIF 最小单元审计与32篇复验期`
- `C1` 职责：只判断每条说法是不是最小单元，还是有限组合、旧容器、观察项或暂不能判
- `C1` 不做：功能归属判断、不新增概念、不改名、不补新条目

## 当前冻结口径核对

| 口径项 | 核对结果 |
|--------|---------|
| 当前正式阶段 | `IIF 最小单元审计与32篇复验期` |
| 本轮角色 | `C1_结构裁判中心` |
| 主裁判范围 | 仅 `42` 条主审计清单 |
| `IIE` 参考附录 | `17` 条，只作参考，不并列主裁判 |
| 允许结论 | 仅 `最小单元，可保留 / 有限组合，不得再按单条冻结 / 旧容器，不是最小单元 / 观察项，暂不升格 / 暂不能判` |
| 当前关注点 | 结构可拆性、多动作/多判断/多路线约束打包、旧路线多步动作是否被包成单条 |

## 禁止项核对

- [x] 不恢复 `6槽位`
- [x] 不新增任何平行概念
- [x] 不改名、不补新条目
- [x] 不替 `C2` 做功能裁判
- [x] 不把 `17` 条 `IIE` 参考附录当主裁判清单
- [x] 不写桥接、生成、反推方案

## 当前交付物

| 文件 | 状态 | 路径 |
|------|------|------|
| 00_状态卡.md | 已交付 | 本文件 |
| 01_最小单元结构裁判.md | 已交付 | 同目录 |

## 结构裁判结果摘要

| 结论档位 | 条数 |
|----------|------|
| 最小单元，可保留 | 23 |
| 有限组合，不得再按单条冻结 | 9 |
| 旧容器，不是最小单元 | 7 |
| 观察项，暂不升格 | 3 |
| 暂不能判 | 0 |

补充说明：

1. 保留项主要集中在单一动作、单一规则、单一构件和少数单一架构原则。
2. 降为“有限组合”的条目，主要是把两步以上动作、前后联动或多层推进捆成一条。
3. 降为“旧容器”的条目，主要是显式簇、分级框架或泛桶式载体。
4. `OBS-01~OBS-03` 继续按观察项处理，本轮不升格为冻结单条。

## 当前下一步

1. 等待 `C2_功能裁判中心` 对同一 `42` 条主审计清单完成功能裁判。
2. `C0_审计整编中心` 在 `C1/C2` 结果齐备后，用经审计后的有限单元集合回看 `32` 篇样本。
3. `IIF` 末端再由实验中心汇总形成阶段裁决；本文件不提前代做总裁决。
