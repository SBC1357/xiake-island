# IIA-IIB中间环节讨论结果与阶段决议

状态：已确认  
日期：2026-04-03  
用途：只记录 `IIA -> IIB` 过渡期间已经被确认的阶段决议，不重复展开完整讨论过程

## 证据摘要

- 已直接核对本文件只写入本线程已经确认的决议，不写未定事项。
- 已直接核对这些决议已分别在正式文档中落点：[藏经阁条件性对象表_20260403.md](../../00_实验中心/藏经阁条件性对象表_20260403.md)、[藏经阁IIA期实验方案_20260403.md](../../01_实验方案/藏经阁IIA期实验方案_20260403.md)、[阶段过渡操作规则.md](../00_阶段过渡操作规则.md)。
- 已直接核对 `docs/多Agent藏经阁实验` 内旧术语残留已清理，当前统一使用 `人格`。

## 一、术语决议

1. 正式术语统一为 `人格`
2. `人格` 取代旧叫法，但层级定位不变
3. `人格` 继续定义为上层组合映射，不是底层第五层

## 二、表结构决议

1. `条件性对象 / 稿型子型` 不作为新层级
2. 它们不继续散落在主对象表中
3. 单列一张 `条件性对象表`
4. 该表是独立二维边界表，不是单列枚举

## 三、阶段判断决议

1. 当前已从 `IIA 主体执行` 进入 `IIA -> IIB 中间环节`
2. `条件性对象表` 属于 `IIA 收口补件`
3. 为了立这张表，不需要临时扩大样本量
4. 弱证据条目留到 `IIB` 扩样复核

## 四、IIB 开工决议

1. `IIB` 不直接开正式主实验
2. 先做候选样本筛选
3. 各中心先提候选
4. 正式名单由总协调统一冻结

## 五、目录机制决议

1. 从本轮开始，实验正式引入 `03_阶段过渡`
2. 每个正式阶段结束后，都建立一个 `<前阶段>-<后阶段>中间环节` 目录
3. 该目录固定收纳：
   - 讨论过程
   - 讨论结果
   - 补充执行文档
   - 待执行事项
