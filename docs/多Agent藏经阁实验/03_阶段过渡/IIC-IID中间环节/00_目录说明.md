# IIC-IID中间环节目录说明

状态：Active  
日期：2026-04-04  
用途：收纳 `IIC -> IID` 过渡期间关于 `IIC` 结论降级、重测口径迁移和 `IID` 重测准备的讨论与执行文档

## 收纳范围

本目录只收：

1. `IIC` 结论降级说明
2. `IIC -> IID` 阶段决议
3. `IID` 重测设计原则
4. 当前待执行事项

本目录不收：

1. 三中心 `IIC` 正式结果
2. `IID` 冻结版正式实验方案
3. 后续总审终点评估

这些内容仍分别进入：

1. [02_阶段结果/IIC期](../../02_阶段结果/IIC期)
2. [01_实验方案](../../01_实验方案)

## 当前一句话口径

`IIC` 保留，但结论降级为“结构化素材执行测试”；真正乱序、去编排、去显性分组的重测，改到 `IID` 执行。
