# IIC-IID中间环节待执行事项

状态：进行中  
日期：2026-04-04  
用途：记录 `IIC -> IID` 过渡阶段当前仍待完成的事项

## 证据摘要

- 已直接核对 `IIC` 三中心结果已落盘，但当前应降级解读。
- 已直接核对 `IIC` 正式题包存在不同程度的结构化编排问题。
- 当前线程已明确确认：`IIC` 保留，但结论降级；真正重测改到 `IID`。
- 当前线程已明确确认：`IID` 的题面应尽量模拟日常真实写稿入口，即 `素材 + 限制条件 + 开放性要求`。
- 已直接回读 `IID` 首版四卷，确认素材量仍然过厚，需改成薄卷版。

## 待执行

1. 冻结 `IIC` 降级说明
   - 状态：Completed
2. 冻结 `IIC -> IID` 过渡决议
   - 状态：Completed
3. 冻结 `IID` 重测设计原则
   - 状态：Completed
4. 在总协调入口与结果导航中同步“`IIC` 保留但降级”的口径
   - 状态：Completed
5. 起草 `IID` 正式实验方案
   - 状态：Completed
6. 为 `IID` 重新制作“真实需求入口”测试包
   - 状态：Completed
7. 给 `IID` 冻结需求模板
   - 包括：`素材 / 限制条件 / 开放性要求`
   - 状态：Completed
8. 将 `IID` 四卷改成薄卷版
   - 重点改成：`会不会写 / 会不会删 / 会不会撑 / 会不会推开`
   - 状态：Completed
9. 下发三中心执行指令
   - 状态：Pending
