# IIE-IIF中间环节待执行事项

状态：进行中  
日期：2026-04-04  
用途：记录 `IIE -> IIF` 过渡阶段当前仍待完成的事项；本文件替代原先分散在独立执行文档里的待办。

## 证据摘要

- 已直接核对 `IIE` 批次 A / 批次 B / 抽样复核结果均已进入结果区。
- 当前线程已明确确认：`IIE` 提前终止，`IIF` 正式启动。
- 当前线程已明确确认：后续过渡文件应继续放在 `IIE-IIF` 目录，而不再继续放在 `IID-后续推进中间环节`。

## 待执行

1. 已完成：`IIE` 提前终止；现有 `IIE` 结果整体保留为证据区
   - 状态：Completed
2. 已完成：`IIF` 正式方案与 `IIF` 结果状态卡已建立
   - 状态：Completed
3. 待执行：`C0` 整编 `IIE` 之前所有发现过的说法，形成最小单元审计底稿
   - 状态：Pending
4. 待执行：`C1` 对前置说法做结构裁判，判断最小单元 / 组合态 / 旧容器 / 案例表达
   - 状态：Pending
5. 待执行：`C2` 对前置说法做功能裁判，判断写作目的候选 / 条件性对象候选 / 稿型子型候选 / 旧母路线容器 / 暂不能判
   - 状态：Pending
6. 待执行：在最小单元审计完成后，用 `IIE` 的 `32` 篇样本再验证一轮
   - 状态：Pending
7. 待执行：形成 `IIF` 汇总结论，并据此裁决 `II期` 是否继续、是否失败重开
   - 状态：Pending
8. 如需新增阶段决议，补写到 [02_讨论结果与阶段决议.md](02_讨论结果与阶段决议.md)
   - 状态：Pending
