# 藏经阁资产盘点总表

状态：当前共享总表  
日期：2026-04-05  
用途：作为 `15` 个公式位、`8` 公式当前成立状态与外围正式资产的上游盘点总表；后续凡共享冻结、升格、拆分、合并、淘汰，除原阶段结果文件外，也同步更新本表

## 证据摘要

- 已直接核对 [藏经阁假设建设逻辑.md](藏经阁假设建设逻辑.md) 与 [I期/01_三中心交叉裁决表.md](../02_阶段实验结果/I期/01_三中心交叉裁决表.md)，确认当前主链就是 `8` 条公式、`15` 个公式位，以及 `OBJ / PUR / ARR / MAP`、`5 + 5` 规则、`5 + 5 + 5` 形态的共享冻结版。
- 已直接核对 [I期/02_L1L2资产入表交叉裁决表.md](../02_阶段实验结果/I期/02_L1L2资产入表交叉裁决表.md) 与 [I期/03_逐项接线交叉裁决表.md](../02_阶段实验结果/I期/03_逐项接线交叉裁决表.md)，确认 `L1/L2.5` 文件级入表结果、第三段已闭合资产、待组合资产与候选池。
- 已直接核对 [II期/03_题型验证结果.md](../02_阶段实验结果/II期/03_题型验证结果.md)、[II期/04_阶段结论.md](../02_阶段实验结果/II期/04_阶段结论.md)、[C1_归类中心/07_逐项接线冻结.md](../03_过程强留痕/I期/C1_归类中心/07_逐项接线冻结.md) 与 [09_项目文稿补充样本.json](../../03_阶段过渡/IID-后续推进中间环节/09_项目文稿补充样本.json)，确认 `IIB` 已结束、当前 `15/15` 位已显式共享落表，以及 `最小有效内容单元 / 最小有效逻辑单元` 可按现有正式单元与范围映射表回填，不需要另起平行编号体系。

## 使用规则

1. 本表只做上游盘点，不替代当前阶段结果文件。
2. 如果本表与当前阶段正式结果冲突，以当前阶段正式结果为准；处理完结果文件后，必须回补本表。
3. 对于当前正式冻结表已冻结数量与归位、但未逐条重列命名的位，本表按三中心一致命名实例回填；后续如出现新的正式重命名，以新正式结果覆盖本表。

## 当前总判断

1. 当前 `15` 个公式位里，已达到 `15/15` 显式共享落表。
2. `IIB` 已补平：
   - `最小有效内容单元`
   - `最小有效逻辑单元`
3. `IIB` 已结束，`IIC` 待进入；人物真相线已写死为单路，决策辅助线已写死为双路。
4. 原藏经阁正式文件资产共 `26` 个，已全部完成 `L1 / L2.5` 入表，但它们不直接并入本表的 `8` 公式主链。

## 一、`8` 公式当前成立状态

| 公式 | 当前状态 | 当前判断 |
| --- | --- | --- |
| `1. 写作目标 * 排列规则 = 写作目的` | 已冻结 | `OBJ-01~08` 与 `ARR-01~07` 已形成共享上游输入，`PUR-01~07` 已冻结；`MAP-01~03` 作为附属计算规律已冻结。 |
| `2. 写作目的 * 人格 = 写作范围` | 已冻结并经 `IIA` 补平 | `PUR-01~07 + ARCH-02` 已支撑 `RANGE-01~05`；其中 `RANGE-03` 已写死为单路，`RANGE-05` 已写死为双路。 |
| `3. 写作范围 * 最小内容单元 = 最小有效内容单元` | 已显式共享落表 | `最小有效内容单元` 已按 `RANGE-01 / 02 / 03 / 04 / 05` 及 `RANGE-05` 双支映射写死，不再只靠结果位隐含倒推。 |
| `4. 写作范围 * 最小逻辑单元 = 最小有效逻辑单元` | 已显式共享落表 | `最小有效逻辑单元` 已按“`ARCH-04` 共享底盘 + `RANGE-03` 主从约束 + `RANGE-05` 分流约束”映射写死。 |
| `5. 最小有效内容单元 * 内容单元组合规则 = 内容组合` | 已稳定并补平输入落点 | 当前 `5` 组内容组合已稳定存在，且其内容输入已反压成正式共享的 `最小有效内容单元` 映射表。 |
| `6. 最小有效逻辑单元 * 逻辑组合规则 = 逻辑组合` | 已稳定并补平输入落点 | 当前 `5` 组逻辑组合已稳定存在，且其逻辑输入已反压成正式共享的 `最小有效逻辑单元` 映射表。 |
| `7. 内容组合 * 逻辑组合 = 文章大纲` | 已稳定 | `OUT-01~05` 都已成立；`OUT-03` 已从待组合改判为单路写死。 |
| `8. 写作范围 * 文章大纲 = 有效大纲` | 已稳定 | `E-OUT-01~05` 都已成立；`E-OUT-03` 已单路写死，`E-OUT-05` 当前按双路运行。 |

## 二、`15` 个公式位资产盘点

### 2.1 上游输入位

1. `写作目标`
   - 状态：已冻结
   - 当前正式资产：
     - `OBJ-01` 还原决策逻辑
     - `OBJ-02` 赋予判断标尺
     - `OBJ-03` 重建认知坐标
     - `OBJ-04` 解码战略路径
     - `OBJ-05` 还原人物内核
     - `OBJ-06` 机制→价值翻译
     - `OBJ-07` 证据→判断转译
     - `OBJ-08` 非常规照亮
2. `排列规则`
   - 状态：已冻结
   - 当前正式资产：
     - `ARR-01` 逐级翻译
     - `ARR-02` 框架先行
     - `ARR-03` 层层剥开
     - `ARR-04` 旧→新对位
     - `ARR-05` 行为反推
     - `ARR-06` 数据管道
     - `ARR-07` 偏差引入
   - 当前附属计算规律：
     - `MAP-01` 一对一主通道
     - `MAP-02` 同目标多目的
     - `MAP-03` 同目的多源头
3. `写作目的`
   - 状态：已冻结
   - 当前正式资产：
     - `PUR-01` 裁决权转移
     - `PUR-02` 坐标系重置
     - `PUR-03` 决策必然性还原
     - `PUR-04` 证据→判断管道
     - `PUR-05` 人物驱动力解码
     - `PUR-06` 机制价值化
     - `PUR-07` 反常识引爆
4. `人格`
   - 状态：已冻结
   - 当前正式资产：
     - `ARCH-02` 人格为上层组合映射

### 2.2 中游约束位

5. `写作范围`
   - 状态：已冻结并经 `IIA` 补平
   - 当前正式资产：
     - `RANGE-01` 评价-解释 / 评价-工具 / 评价-建构范围
     - `RANGE-02` 主张-论证范围
     - `RANGE-03` 人物真相探索范围
       - 当前正式写法：单路
     - `RANGE-04` 工程解释降门槛范围
     - `RANGE-05` 决策辅助范围
       - 当前正式写法：双路
       - 当前双路：`结果边界澄清` / `患者选择沟通`
6. `最小内容单元`
   - 状态：已冻结
   - 当前正式资产：
     - `ACT-04` 解释桥
     - `ACT-05` 比喻承重
     - `CMP-01` 一条硬事实
     - `CMP-02` 一个判断
     - `CMP-06` 一条边界声明
     - `CO-03` 数据锤击
     - `CO-04` 权威锚定
     - `CO-05` 风险解剖
   - 去重说明：
     - `CO-01` 视为 `ACT-05` 别名
     - `CO-02` 视为 `ACT-04` 别名
7. `最小逻辑单元`
   - 状态：已冻结并经 `IIA` 扩充
   - 当前正式资产 / 正式判断：
     - `ARCH-04` 级联/平行是内容拓扑
     - `人物线 / 问题线主从`
     - `人群分流 / 选择建议落点`
       - 当前正式结果：双路判断
       - 当前双路：`结果边界澄清` / `患者选择沟通`
8. `最小有效内容单元`
   - 状态：已显式共享落表
   - 当前正式写法：
     - 不另起新编号，按“范围 / 支路 -> 有效内容输入”共享映射表写死
   - 当前共享映射：
     - `RANGE-01 / RANGE-02`：`一条硬事实`、`一个判断`、`一条边界声明`、`数据锤击`、`权威锚定`
     - `RANGE-03`：`一条硬事实`、`一个判断`、`权威锚定`
     - `RANGE-04`：`解释桥`、`比喻承重`、`一条硬事实`、`数据锤击`、`一个判断`、`一条边界声明`
     - `RANGE-05 / 结果边界澄清`：`一个判断`、`一条边界声明`、`风险解剖`、`数据锤击`、`权威锚定`
     - `RANGE-05 / 患者选择沟通`：`一个判断`、`一条边界声明`、`风险解剖`、`解释桥`
   - 压缩判断：
     - `一个判断` 是当前五路共享底盘
     - `解释桥` 已从 `RANGE-04` 的局部有效项，扩成 `RANGE-05` 患者沟通支也会稳定调用的有效输入
     - `比喻承重` 当前仍只在 `RANGE-04` 稳定必要
     - `风险解剖` 当前仍只在 `RANGE-05` 双路稳定必要
9. `最小有效逻辑单元`
   - 状态：已显式共享落表
   - 当前正式写法：
     - 不另起新编号，按“范围 / 支路 -> 有效逻辑输入”共享映射表写死
   - 当前共享映射：
     - `RANGE-01 / RANGE-02 / RANGE-04`：`ARCH-04`
     - `RANGE-03`：`ARCH-04 + 人物线 / 问题线主从`
     - `RANGE-05 / 结果边界澄清`：`ARCH-04 + 人群分流 / 选择建议落点`
     - `RANGE-05 / 患者选择沟通`：`ARCH-04 + 人群分流 / 选择建议落点`
   - 压缩判断：
     - `ARCH-04` 是当前全路线共享逻辑底盘
     - `人物线 / 问题线主从` 当前只对 `RANGE-03` 有效
     - `人群分流 / 选择建议落点` 当前只对 `RANGE-05` 双路有效
10. `内容单元组合规则`
   - 状态：已冻结
   - 当前正式资产：
     - 事实先于判断，判断必须被购买
     - 判断体量与细节成本同步抬升
     - 边界、失败、风险必须进入正文购买链
     - 抽象机制落地时，解释桥或承重比喻必须贴着对象出场
     - 收尾判断不得高于正文已买到的最大判断
11. `逻辑组合规则`
   - 状态：已冻结
   - 当前正式资产：
     - 评价尺子或判断坐标必须先立，再进入比较或裁决
     - 主问题单轴时走级联；比较对象并列时走平行或对照推进
     - 对比、反讽、对切必须建立共享参照面后再启动
     - 阳性结果、唯一性、标杆判断必须先做来源还原或适用边界分流
     - 多步逻辑来源只能拆件嵌入主线，不能整包照搬

### 2.3 结果位

12. `内容组合`
   - 状态：已稳定
   - 当前正式资产：
     - 评价尺子 + 硬事实 / 数据 / 边界 + 结论回收
     - 旧分类不足 + 分类 / 结构证据 + 主张收束
     - 人物行动证据 / 技术行业经历插层 / 行动逻辑回收
     - 需求 / 标准 + 解法验证 + 工程或临床意义
     - 阳性结果 / 显著结论 + 来源 / 人群 / 场景还原 + 边界分流
13. `逻辑组合`
   - 状态：已稳定
   - 当前正式资产：
     - 先立尺子后按级联 / 平行推进比较
     - 先否旧定义后重写坐标并收定义权
     - 先设人物谜题后沿人物线 / 问题线回钻
     - 先列需求后逐项对照解法
     - 先承认结果后逆拆来源与适用边界
14. `文章大纲`
   - 状态：已稳定
   - 当前正式资产：
     - `OUT-01` 尺子前置-分层递进型
     - `OUT-02` 重定义-收束主张型
     - `OUT-03` 人物谜题-真相回钻型
       - 当前正式写法：单路
     - `OUT-04` 需求-解法对照型
     - `OUT-05` 结果前置-来源还原-边界界定型
15. `有效大纲`
   - 状态：已稳定
   - 当前正式资产：
     - `E-OUT-01` 专业评判型有效大纲
     - `E-OUT-02` 强主张型有效大纲
     - `E-OUT-03` 人物真相型有效大纲
       - 当前正式写法：单路
     - `E-OUT-04` 工程解释型有效大纲
     - `E-OUT-05` 决策辅助型有效大纲
       - 当前正式写法：双路
       - 当前双路：`结果边界澄清` / `患者选择沟通`

## 三、当前仍只算候选或证据位的对象

1. 当前共享尾项已补平；主链不再存在“只在结果位隐含存在、但未显式落表”的公式位。
2. 仍保留在观察或证据位、暂不升格为共享正式资产的对象包括：
   - `CMP-03 / CMP-04 / CMP-05` 的拆件结果
   - `OBS-01 / OBS-02 / OBS-03`
   - `GS-02 ~ GS-06`
   - `CO-06 / CO-07`
   - `ACT-03②`
   - `ACT-07②`
   - `RUL-05②`
3. 这些对象当前只作为证据或扩展池使用，不直接改写本表主链。

## 四、外围正式资产

### 4.1 `L1` 表达层（`16`）

1. `expression_base.json`
2. `lighthouse_banfoxianren.json`
3. `lighthouse_bingdian.json`
4. `lighthouse_chengongzi.json`
5. `lighthouse_dailaoban.json`
6. `lighthouse_kongzhike.json`
7. `lighthouse_renwu.json`
8. `lighthouse_shenlanguan.json`
9. `lighthouse_zhangjiaw.json`
10. `m1_argument_logic_rules.json`
11. `m2_narrative_primitives.json`
12. `m4_rhetoric_blocks.json`
13. `native_syntax_rules.json`
14. `persona_kernels.json`
15. `register_levels.json`
16. `style_dna_schema.json`

### 4.2 `L2.5` 医学映射层（`10`）

1. `m0_persona_templates.json`
2. `m3_strategic_plays.json`
3. `m4_medical_blocks.json`
4. `m5_compliance_schema.json`
5. `medical_expression_profiles.json`
6. `medical_syntax_rules.json`
7. `narrative_arcs.json`
8. `persona_medical_overlay.json`
9. `rhetoric_blocks.json`
10. `sentence_patterns.json`

## 五、后续更新规则

1. 后续凡出现共享冻结、升格、拆分、合并、淘汰，必须先写原阶段正式结果文件，再同步回写本表。
2. 如果只是聊天里的猜想、过程推理或尚未达阈值的候选，不直接改本表主链，只能先留在当前阶段结果文件或 [00_总留痕.md](../03_过程强留痕/00_总留痕.md)。
3. `IIB` 的默认盘点补位目标已完成：
   - `最小有效内容单元`
   - `最小有效逻辑单元`
4. 当前不恢复旧 `II期` 母框架，不启动 `IIF` 式 `32` 篇逐篇复验，也不另起平行概念。

