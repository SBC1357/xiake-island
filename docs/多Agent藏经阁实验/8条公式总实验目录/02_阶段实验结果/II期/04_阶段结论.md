# II期 / 04_阶段结论

状态：当前结论 / `IIF` 已完成全部 4 臂 17 次运行，因果裁决与系统上限裁决已落盘  
日期：2026-04-07  
阶段：II期  
用途：承接 `II期` 的阶段性冻结、升格、淘汰、拆分、延期与通过判断；没有足够证据时，不在这里提前写死结论

## 证据摘要

- 已直接核对 [藏经阁II期实验方案_20260405.md](../../01_阶段实验方法/藏经阁II期实验方案_20260405.md)，确认 `II期` 当前只负责验证与回压共享母表，不另起旧 `II期` 母框架。
- 已直接核对 [03_过程强留痕/00_总留痕.md](../../03_过程强留痕/00_总留痕.md)，确认任何升格、淘汰、推翻都必须同步写回总留痕。
- 已直接核对 [02_扩样结果.md](02_扩样结果.md)、[03_题型验证结果.md](03_题型验证结果.md) 与 [00_实验总纲/藏经阁资产盘点总表.md](../../00_实验总纲/藏经阁资产盘点总表.md)，确认当前结论都已有扩样证据或题型证据支撑，且 `IIB` 的两个尾项已同步反压回上游盘点表。
- 已直接核对 [00_状态卡.md](00_状态卡.md)、[II期成果整理/01_结果分析.md](../../../II期成果整理/01_结果分析.md)、白盒 runtime 目录与 `run_ii_whitebox_draft.ps1 / run_ii_whitebox_orchestrator.ps1 / run_ii_whitebox_score.ps1`，确认 `IIC` 及后续白盒工程结果已经足以做去污染后的阶段裁决，且当前链路是“闭卷出稿、出稿后再参考答案对照评分”：`IIC` 未冻结，`IID / IIE / IIF` 未正式收口，`III期` 未启动。

## 当前状态

1. `IIA` 与 `IIB` 的阶段结论都已形成，并已达到写回本文件的阈值。
2. 当前结论已覆盖 `IIA` 的对象收口结果与 `IIB` 的中间层补平结果，但不替代后续 `IIC / IID` 的任务。
3. 当前未触发 `I期` 硬错回退路径。

## 一、对象级结论

1. `人物线 / 问题线主从`
   - 结论：从共享候选升格为共享正式最小逻辑单元。
   - 依据：人物向扩样 `3/3` 稳定重复；`OUT-03 / E-OUT-03 / RANGE-03` 的接线题与大纲重建题 `3/3` 都稳定接到同一组下层关系。
2. `OUT-03 / E-OUT-03 / RANGE-03`
   - 结论：从“正式但待组合”改判为“已写死为单路”。
   - 当前正式写法：`RANGE-03 + 人物行动证据 / 技术行业经历插层 / 行动逻辑回收 + 先设人物谜题后沿人物线 / 问题线回钻`。
3. `人群分流 / 选择建议落点`
   - 结论：从共享候选改判为共享正式最小逻辑单元判断，当前正式结果不是压成一支，而是稳定拉成两支。
   - 当前正式写法：`结果边界澄清` 与 `患者选择沟通` 双支并存。
4. `RANGE-05 / E-OUT-05`
   - 结论：从“正式但待组合”改判为“已写死为双路”。
   - 当前正式写法：
     - 专业边界澄清支：继续沿 `OUT-05` 的“结果前置 / 来源还原 / 适用边界”主骨架运行。
     - 患者选择沟通支：最终必须落到选择建议、替代方案或风险预期，不再强压回单一路线。

## 二、I期 回退判断

1. 本轮未发现 `I期` 已冻结结果存在硬错。
2. 当前抽样复核表明：
   - `RANGE-01` 及其对应已冻结组合链仍能稳定回拆与正推。
   - `OUT-05` 的文章大纲层仍稳定；当前被裁决的是 `RANGE-05 / E-OUT-05` 的范围层与有效大纲层双路问题，不是 `OUT-05` 自身翻案。
3. 因此，本轮不触发“发现 `I期` 硬错”的回退路径。

## 三、IIA 阶段结论

1. `IIA` 当前最重要的共享阻塞位已经完成首轮收口：
   - 人物真相线已从候选转为正式。
   - 决策辅助线已从“待组合”转为“双路写死”。
2. `15` 个公式位里，当前仍未显式落表的只剩：
   - `最小有效内容单元`
   - `最小有效逻辑单元`
3. 这两个位当前已经从“会卡主线的结构缺口”降为“需要在下一子阶段补共享落点的文档尾项”。
4. `IIA` 到此结束；下一步应进入 `IIB`，优先做：
   - `最小有效内容单元 / 最小有效逻辑单元` 的显式共享落点补平
   - 当前双路对象的组合重建题
   - 更接近真实任务的题型过渡

## 四、IIB 阶段结论

1. `最小有效内容单元`
   - 结论：已从“结果位隐含存在”改判为“显式共享落表”。
   - 当前正式写法：不另起新编号，按“范围 / 支路 -> 有效内容输入”映射写死。
   - 当前共享映射：
     - `RANGE-01 / RANGE-02`：`一条硬事实`、`一个判断`、`一条边界声明`、`数据锤击`、`权威锚定`
     - `RANGE-03`：`一条硬事实`、`一个判断`、`权威锚定`
     - `RANGE-04`：`解释桥`、`比喻承重`、`一条硬事实`、`数据锤击`、`一个判断`、`一条边界声明`
     - `RANGE-05 / 结果边界澄清`：`一个判断`、`一条边界声明`、`风险解剖`、`数据锤击`、`权威锚定`
     - `RANGE-05 / 患者选择沟通`：`一个判断`、`一条边界声明`、`风险解剖`、`解释桥`
2. `最小有效逻辑单元`
   - 结论：已从“结果位隐含存在”改判为“显式共享落表”。
   - 当前正式写法：不另起新编号，按“范围 / 支路 -> 有效逻辑输入”映射写死。
   - 当前共享映射：
     - `RANGE-01 / RANGE-02 / RANGE-04`：`ARCH-04`
     - `RANGE-03`：`ARCH-04 + 人物线 / 问题线主从`
     - `RANGE-05 / 结果边界澄清`：`ARCH-04 + 人群分流 / 选择建议落点`
     - `RANGE-05 / 患者选择沟通`：`ARCH-04 + 人群分流 / 选择建议落点`
3. 本轮未触发定向补样。
   - 原因：当前控制组、单路组、双路组的重建结果已足以判清共享有效与局部有效边界。
4. 当前 `15` 个公式位已达到 `15/15` 显式共享落表。
5. `IIB` 到此结束；当前已具备进入 `IIC` 的前置条件：
   - 中间层已可稳定回拆
   - 组合已可稳定重建
   - 失败已可回指到内容位或逻辑位，而不再只停在上层结果位

## 五、`IIC` 及以后当前裁决（去污染修正）

1. 去污染口径：
   - `II期` 主阶段只认 `IIC -> IID -> IIE -> IIF`
   - `2026-04-06` 白盒目录里的 `Round 1 / Round 2 / Round 3 / Round 4 / 5轮回路` 只代表白盒局部工程运行标签，不得升格成 `II期` 主阶段
2. `IIC`
   - 结论：实验执行已完成；当前阶段判断为“已完成、未冻结、转入 `IID`”
   - 依据：两道 `C类` 外部闭卷题在补看稿面后都判失败；两道 `D类` 噪声题只属诊断轮，不计正式通过；但后续人工终审补证已经结束
   - 补充裁决：`Round 2` 的提分点不是靠出稿前看见参考答案才生效；当前出稿链里，这组“题型锁定 + 关键事实锁定 + 结构锁定”字段本身可以在无 gold 条件下执行
   - 当前边界：现行白盒链的正式发现与裁决仍依赖“闭卷出稿后再做参考答案对照评分”与人工审稿；因此系统目前已具备规则复用能力，但还不具备无 gold 的自主纠偏能力
   - 人工终审补证：用户已明确给出正式评价，当前 `4` 篇通过稿质量确实很高；这批稿件可视为 `IIC` 已跑出的真实优质正样本
   - 阶段含义：`IIC` 的问题已经不再是“有没有跑出好稿”，而是“怎样把这 `4` 篇好稿反推成系统优化、PDF阅读链与无 gold 审核标准”
3. `IIC` 之后已出现的白盒工程证据：
   - 烟测 `0/7`
   - `ii_whitebox_phase_round2` 为 `4/7`
   - 后续 `5` 次全量完成 run 为 `1/7`、`4/7`、`4/7`、`2/7`、`2/7`
   - 这些结果只能说明白盒底盘已能跑、局部能力有提升，但稳定性不足，不能替代 `IIC / IIF` 正式通过
4. `IID`
   - 结论：已于 `2026-04-06` 正式完成
   - 当前正式定义：缺位审计 + 失败归因重分类 + 证据清理 + A/B 协议冻结
   - 交付资产：[IID_8公式缺位矩阵.md](IID_8公式缺位矩阵.md)、[IID_IIC失败样式重归因表.md](IID_IIC失败样式重归因表.md)、[IID_AB对照协议.md](IID_AB对照协议.md)、[IID_formula_trace_模板.md](IID_formula_trace_模板.md)
   - 证据状态：
     - Q1：仅为"管线可启动 / 部分工程证据"，不是 `8` 公式因果证据
     - Q2：**证据失效**（Agent 代写，非白盒 runtime），已从因果证据链移除
     - Q3：**证据失效**（Agent 代写，非白盒 runtime），已从因果证据链移除
     - Q4：旧 blocked 样稿组记录保留，不是当前主执行路径
   - 当前没有任何题能同时满足：真闭卷 + 真 `8` 公式调用 + 真高质量
5. `IIE`
   - 结论：已于 `2026-04-07` 正式通过收口
   - 通过依据：
    - (a) 首批关键公式位已编码进白盒管线 4 层（orchestrator/materials/draft/score）：当前 regression runtime 可直接核到 `formula_contract` 的 `12` 个 machine-readable 键，prompt 层可见 `写作目的 / 排列规则`，score 层已上线 `formula_trace / formula_compliance`；5 个文件修改，4 个脚本全部 PowerShell 解析通过
     - (b) A/B prompt 差分模板已冻结：[IIE_A_prompt_模板.md](IIE_A_prompt_模板.md)、[IIE_B_prompt_模板.md](IIE_B_prompt_模板.md)、[IIE_A_B差分说明.md](IIE_A_B差分说明.md) 共 8 条差分逐条可审计
     - (c) `IIC` 失败题搭建回归完成 2 题：
       - `iie_reg_patient_c`：72 分 / qualified / formula_trace 16 条
       - `iie_reg_mechanism_c`：72 分 / qualified / formula_trace 17 条
     - (d) 两题闭卷条件确认：`reference_answer_visible_before_draft=false`
   - formula_compliance=65（两题均未达 80 阈值），说明公式编码已在起作用但命中率约 70%，关键 miss 集中在 effective_content_unit 的精确值呈现和数据锤击
   - 对比 IIC 失败样式：persona/effective_outline 已修复，content_combo 部分修复，exact value 入链未完全修复
   - 当前判断：管线搭建阶段目标已达成，剩余优化属于 IIF 正式验证范畴
   - runtime 产物：`D:\汇度编辑部1\侠客岛-runtime\ii_whitebox\20260406_234421_946_iie_reg_patient_c\` 与 `D:\汇度编辑部1\侠客岛-runtime\ii_whitebox\20260406_234656_267_iie_reg_mechanism_c\`
6. `IIF`
   - 结论：已于 `2026-04-07` 完成全部 4 臂 17 次白盒管线运行
   - 实验设计：4 臂（因果验证 / 样稿增益 / revise loop / 系统上限），严格按方案第六节执行序列
   - **IIF-1 因果性裁决：不成立**
     - A 组（无公式）3 题均值=62.67，B 组（有公式）3 题均值=66.0，Δ=+3.33 < 5 阈值
     - B 组 formula_compliance 均值=56.33 < 70 阈值
     - 在 3 题样本下，公式编码未带来显著成稿质量提升
   - **IIF-2 样稿增益裁决：成立**
     - baseline（公式+无样稿）2 题均值=65，experiment（公式+样稿）2 题均值=78，Δ=+13
     - 样稿作为参考输入能显著提升成稿质量
   - **IIF-3 Revise loop 裁决：不成立**
     - baseline（公式+样稿，无 revise）2 题均值=75，experiment（公式+样稿+revise）2 题终态均值=75，Δ=0
     - 当前 revise loop 未带来增益，且个别题出现回退（patient: 78→72），说明评分→修改循环需要重新设计
   - **IIF-4 系统上限裁决：有价值底稿水平**
     - 3 题 full-open（公式+样稿+revise）终态均值=76.0
     - 0/3 ≥ 85（不可交付），2/3 ≥ 75（有价值底稿），3/3 ≥ 70（合格底稿）
     - 当前系统在最优条件下能产出有编辑价值的底稿，但不能直接交付
   - 正式成果文件：[IIF_因果性结论.md](IIF成果整理/IIF_因果性结论.md)、[IIF_系统上限结论.md](IIF成果整理/IIF_系统上限结论.md)
   - runtime 产物：17 条 run 均在 `D:\汇度编辑部1\侠客岛-runtime\ii_whitebox\` 下
   - 已知工程缺陷：`run_ii_whitebox_revise.ps1` 第 430 行 cosmetic 报错，不影响实验结果
7. `III期`
   - 结论：当前不得进入
   - 依据：总纲与 `II期` 方案都写死 `III期` 前置条件是完成 `IID -> IIE -> IIF`；而当前连 `IIC` 都未冻结，`IIF` 也未启动
8. 当前下一阶段目标：
   - 不是 `III期`（需用户审阅 IIF 结论后裁决）
   - IIF 已完成，因果性不成立，系统上限=有价值底稿
   - 当前 blocker：无工程 blocker；路线决策待用户
   - IIF 额外发现：样稿增益显著（Δ=+13），revise loop 无效（Δ=0），提示材料质量是主瓶颈、revise 机制需重新设计
