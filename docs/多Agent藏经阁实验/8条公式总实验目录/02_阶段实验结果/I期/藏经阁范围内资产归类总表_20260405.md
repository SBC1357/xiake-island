# 藏经阁范围内资产归类总表_20260405

状态：当前总表  
日期：2026-04-05  
用途：作为“基于 `8` 条公式重整范围内资产”的当前母表；先收确定归位，不在本表里继续养旧 `II期` 母口径；当前已完成前三段冻结，`I期` 只负责把资产理明，未冻结项的验证转入 `II期`

## 证据摘要

- 已直接核对 [IIF-I期中间环节/02_讨论结果与阶段决议.md](../../03_过程强留痕/阶段切换/IIF-I期中间环节/02_讨论结果与阶段决议.md)，确认当前主线是“按 `8` 条公式，把范围内资产重新归类并收回主链”。
- 已直接核对 [藏经阁条件性对象表_旧表快照_20260405.md](../../../98_历史归档/藏经阁条件性对象表_旧表快照_20260405.md)，确认旧表已归档，不再作为当前活跃总表。
- 已直接核对既往 `藏经阁` 目录下代表性资产仍在： [m1_argument_logic_rules.json](../../../../../../藏经阁/l1/writing_craft/m1_argument_logic_rules.json)、[m2_narrative_primitives.json](../../../../../../藏经阁/l1/writing_craft/m2_narrative_primitives.json)、[m3_strategic_plays.json](../../../../../../藏经阁/l2/medical_playbook/m3_strategic_plays.json)、[narrative_arcs.json](../../../../../../藏经阁/l2/medical_playbook/narrative_arcs.json)。
- 已直接核对 [I期/01_三中心交叉裁决表.md](01_三中心交叉裁决表.md)，确认 `I期` 第一轮三中心结果已收成一版共享冻结口径。
- 已直接核对 [I期/03_逐项接线交叉裁决表.md](03_逐项接线交叉裁决表.md)，确认第三段总体评估已经完成，且当前共享未冻结项已被压缩到少数直接阻塞项。
- 已直接核对原藏经阁资产目录存在且可读：`../../../../../../藏经阁/l1/writing_craft`、`../../../../../../藏经阁/l2/medical_playbook`。

## 当前首层归位

| 旧资产族 | 当前归位 | 当前处理 |
| --- | --- | --- |
| 动作 | `最小内容单元` 或 `内容组合` 候选 | 逐条拆看，不能再自动充当母类 |
| 构件 | `最小内容单元` 候选 | 作为内容侧基础候选进入主链 |
| 规则 | `写作范围`、`内容单元组合规则`、`逻辑组合规则` 三侧拆分 | 不再混写成一个旧规则桶 |
| 拓扑 | `最小逻辑单元` 或 `组合逻辑` 候选 | 优先回到逻辑侧，不再单独占旧母位 |
| 人格 | 规则族组合项，经 `写作目的 * 人格 = 写作范围` 接入 | 不丢弃，不后贴 |
| `M4` 医学映射块 | `L2.5` 候选 | 只记映射位置，不升格为新物理层 |
| `register / syntax / expression` | `L1` 表达层 | 本轮不直接并入公式主链 |
| `21 + 14 + 7` 与旧 `ARCH` 口径 | 审计证据 / 旧框架证据 | 只保留证据位，不再充当当前母口径 |

## 当前总协调冻结版

当前以 [I期/01_三中心交叉裁决表.md](01_三中心交叉裁决表.md) 为共享母表，先冻结下面四组结果：

1. 第 `1` 项归类：`20` 个主链直接槽位 + `14` 条拆件来源 + `6` 条停放观察 + `2` 组双入口同构；`ARCH-02` 单独回到 `人格`。
2. 第 `2` 项规则：`5` 条内容单元组合规则 + `5` 条逻辑组合规则；其余扩展条目只保留候选池。
3. 第 `3/4` 项形态：`5` 种文章大纲 + `5` 种有效大纲 + `5` 种写作范围。
4. 第 `5` 项上游类型：`8` 种写作目标 + `7` 种写作目的 + `7` 种排列作用模式 + `3` 条映射规律。

## 第二轮共享冻结版

第二轮已经完成并冻结，当前共享结果如下：

1. 原藏经阁正式资产共 `26` 个文件，全部完成文件级落位。
2. 最终收口为：`16` 个 `L1` 表达层文件 + `10` 个 `L2.5` 医学映射层文件。
3. 第二轮共享冻结版没有新增文件直接进入 `8` 条公式主链，也没有启用证据停放位。
4. 共享冻结口径以 [I期/02_L1L2资产入表交叉裁决表.md](02_L1L2资产入表交叉裁决表.md) 为准。

## 第三段共享冻结版

第三段已经完成总体评估与共享冻结，当前结果如下：

1. 已闭合主线：
   - `4` 种文章大纲
   - `3` 种有效大纲
   - `3` 种写作范围
   - `4` 种内容组合
   - `4` 种组合逻辑
2. 正式但待组合：
   - `OUT-03`
   - `E-OUT-03`
   - `RANGE-03`
   - `E-OUT-05`
   - `RANGE-05`
3. 当前共享候选池只保留 `2` 个最小逻辑单元候选：
   - `人物线 / 问题线主从`
   - `人群分流 / 选择建议落点`
4. 其余单中心扩展池继续保留为证据，不进入当前共享验证主名单。
5. 第三段共享冻结口径以 [I期/03_逐项接线交叉裁决表.md](03_逐项接线交叉裁决表.md) 为准。

## 本轮只收三类结论

1. 已能稳定落到 `8` 条公式某一环的位置
2. 已能明确判定为 `L1` 表达层或审计证据层，不进入本轮主链
3. 暂时不能落位，但原因已写清，需停放观察

## 当前不用本表做的事

1. 不直接派发 `32` 篇复验
2. 不继续压旧 `CO / GS / ARCH` 表
3. 不把工程比喻直接写成正式本体
4. 不在 `I期` 内继续做未冻结项验证；相关验证统一转入 `II期`


