# C0_归类中心 / 06_L1L2资产入表

状态：第二轮完成  
日期：2026-04-05  
中心：C0_归类中心  
用途：作为第二轮唯一主文档，把原藏经阁 `L1/writing_craft` 与 `L2/medical_playbook` 的正式资产文件按当前 `I期` 主链正式入表

## 证据摘要

- 已直接核对第二轮当前正式方案： [藏经阁I期实验方案_20260405.md](../../../01_阶段实验方法/藏经阁I期实验方案_20260405.md)。
- 已直接核对第一轮共享冻结版： [I期/01_三中心交叉裁决表.md](../../../02_阶段实验结果/I期/01_三中心交叉裁决表.md)。
- 已直接核对原藏经阁资产目录：`../../../../../../../藏经阁/l1/writing_craft`、`../../../../../../../藏经阁/l2/medical_playbook`。
- 已直接逐个读取两目录下全部正式 JSON 资产，合计 `26` 个文件，确认无漏扫。

## 当前归位总览

| 类别 | 数量 | 当前判断 |
|---|---:|---|
| `8` 条公式主链 | `0` | 本轮原藏经阁文件不直接升格主链；主链仍只保留第一轮冻结的 `8` 条公式 |
| `L1` 表达层 | `16` | 通用写作表达、句法、人格、原语、修辞、语域与风格底盘 |
| `L2.5` 医学映射层 | `10` | 医学域的人格叠加、叙事/修辞/句式/合规/策略映射 |
| 证据停放位 | `0` | 本轮未发现需要暂存而不入表的正式文件 |

说明：`L2.5` 只作当前工作位，表示“通用写作规则的医学映射层”；不得把它写成新增正式理论层。

## L1 表达层

| 文件 | 当前归位 | 归位理由 | 与第一轮冻结结果的关系 |
|---|---|---|---|
| `expression_base.json` | `L1` 表达层 | 通用表达底盘，明确写的是跨行业可复用的 transition phrases / forbidden expressions | 只作为 `L1` 底盘，不改第一轮 `8` 条公式冻结 |
| `lighthouse_banfoxianren.json` | `L1` 表达层 | 降维解构者人格，沉淀的是通用表达风格而不是医学映射 | 只提供 `L1` 风格实例，不进入主链 |
| `lighthouse_bingdian.json` | `L1` 表达层 | 严肃记者人格，核心是白描、数字、在场感 | 只提供 `L1` 风格实例，不进入主链 |
| `lighthouse_chengongzi.json` | `L1` 表达层 | 临床新闻分析师人格，仍是通用表达风格实例 | 作为 `L1` 风格底盘，不改主链 |
| `lighthouse_dailaoban.json` | `L1` 表达层 | 历史纵深解剖者人格，承接通用叙事气质 | 作为 `L1` 风格底盘，不改主链 |
| `lighthouse_kongzhike.json` | `L1` 表达层 | 冷峻市场解剖者人格，沉淀的是通用写作姿态 | 作为 `L1` 风格底盘，不改主链 |
| `lighthouse_renwu.json` | `L1` 表达层 | 克制的见证者人格，服务人物稿表达 | 作为 `L1` 风格底盘，不改主链 |
| `lighthouse_shenlanguan.json` | `L1` 表达层 | 行业战地记者人格，仍是通用表达实例 | 作为 `L1` 风格底盘，不改主链 |
| `lighthouse_zhangjiaw.json` | `L1` 表达层 | 温柔说书人人格，沉淀的是表达气质与节奏 | 作为 `L1` 风格底盘，不改主链 |
| `m1_argument_logic_rules.json` | `L1` 表达层 | 跨领域立论与论证门禁，约束如何把证据搭成判断 | 为公式输入侧提供门禁，不改第一轮冻结 |
| `m2_narrative_primitives.json` | `L1` 表达层 | 叙事原语集，属于纯写作逻辑底盘 | 只补表达原语，不改 `8` 条公式 |
| `m4_rhetoric_blocks.json` | `L1` 表达层 | 通用修辞积木原型，领域实例由 `L2` 叠加 | 维持在 `L1` 原型层，不升格新理论 |
| `native_syntax_rules.json` | `L1` 表达层 | 通用语法戒律，负责去翻译腔和去机味 | 继续承接表达层，不改主链冻结 |
| `persona_kernels.json` | `L1` 表达层 | 通用人格内核，虽然是公式2“人格”的底盘，但文件本身仍是 L1 底座 | 只提供人格底盘，不把底盘本身当主链 |
| `register_levels.json` | `L1` 表达层 | 语域分级，负责正式度和口语度调节 | 仍属表达层，不改公式冻结 |
| `style_dna_schema.json` | `L1` 表达层 | 只是文风配置结构定义，没有把实例升成新层 | 只提供 schema，不改主链 |

## L2.5 医学映射层

| 文件 | 当前归位 | 归位理由 | 与第一轮冻结结果的关系 |
|---|---|---|---|
| `m0_persona_templates.json` | `L2.5` 医学映射层 | 医学人格模板，明确是在 L1 persona 上叠加医学域配置 | 作为医学映射底盘，不改第一轮 `8` 条公式 |
| `m3_strategic_plays.json` | `L2.5` 医学映射层 | 战略剧本与 play-specific M2 序列，属于医学域的选型/编排层 | 只是医学域映射，不升格新理论层 |
| `m4_medical_blocks.json` | `L2.5` 医学映射层 | 医学修辞积木实例与 M2 映射，直接承担医学域映射职责 | 不是新物理层，只是映射层资产 |
| `m5_compliance_schema.json` | `L2.5` 医学映射层 | 合规映射层，负责把 M4 与最小证据锚点绑定 | 属于医学域约束层，不改主链 |
| `medical_expression_profiles.json` | `L2.5` 医学映射层 | 受众画像与 register/persona 的选择规则，典型映射资产 | 仍只在医学域里做选择，不改 `8` 条公式 |
| `medical_syntax_rules.json` | `L2.5` 医学映射层 | 医学语法增量戒律，是对 L1 语法的领域叠加 | 只扩展医学域表达，不升格理论层 |
| `narrative_arcs.json` | `L2.5` 医学映射层 | 医学叙事弧与作者弧的选型指南，承担场景到结构的映射 | 作为医学映射资源，不改第一轮冻结 |
| `persona_medical_overlay.json` | `L2.5` 医学映射层 | 明确是从 L1 迁移来的医学人格叠加层 | 直接承接 L1 到医学域的映射，不改主链 |
| `rhetoric_blocks.json` | `L2.5` 医学映射层 | 医学修辞积木与隐喻系统，属于医学域实例层 | 只是映射，不是新正式本体 |
| `sentence_patterns.json` | `L2.5` 医学映射层 | 医学句式模式与选型指南，负责把通用句法落到医学域 | 仍是医学映射，不碰第一轮冻结 |

## 旧判断修正留痕

1. 旧判断：`L2.5` 只被口径性地挂在 `M4` 医学映射块上。
2. 触发证据：`medical_playbook` 下实际同时存在 `m0_persona_templates.json`、`m3_strategic_plays.json`、`m4_medical_blocks.json`、`m5_compliance_schema.json`、`medical_expression_profiles.json`、`medical_syntax_rules.json`、`narrative_arcs.json`、`persona_medical_overlay.json`、`rhetoric_blocks.json`、`sentence_patterns.json`，它们共同承担的是医学域映射、选型、合规和叙事调度。
3. 新判断：`L2.5` 应按整套 `medical_playbook` 工作层收口，而不只对应单个 `M4` 块。
4. 影响范围：C0 第二轮把 `medical_playbook` 内 `10` 个正式文件全部收进 `L2.5`，但仍不把 `L2.5` 升格成新理论层。

## 与第一轮冻结结果的关系

1. 第一轮 `8` 条公式冻结不变。
2. 本轮只是补齐 `L1` / `L2.5` 的正式输入底盘，让第一轮冻结结果有可落地的资产承接位。
3. 本轮没有任何文件回写到旧 `II期` 母框架，也没有把第二轮收口改写成新的共享母表。


