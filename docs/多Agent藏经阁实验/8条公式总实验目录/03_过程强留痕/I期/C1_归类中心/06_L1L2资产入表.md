# C1_归类中心 / 06_L1L2资产入表

状态：第二轮完成  
日期：2026-04-05  
中心：C1_归类中心  
用途：把原藏经阁 `L1/writing_craft` 与 `L2/medical_playbook` 的当前正式资产文件，按第一轮冻结后的 `I期` 主链正式入表

## 证据摘要

- 已全量核对原藏经阁正式资产目录，`L1/writing_craft` 共 `16` 个文件，`L2/medical_playbook` 共 `10` 个文件，合计 `26` 个，未发现漏扫子目录。
- 已直接核对 [I期/01_三中心交叉裁决表.md](../../../02_阶段实验结果/I期/01_三中心交叉裁决表.md) 与 [藏经阁I期实验方案_20260405.md](../../../01_阶段实验方法/藏经阁I期实验方案_20260405.md)，确认第一轮 `8` 条公式主链仍是唯一上游主链，本轮不改写主链本体。
- 已直接核对各 JSON 文件的顶层键与内容结构，确认本轮可稳定落入 `L1` 表达层、`L2.5` 医学映射层与证据停放位，不需要恢复旧 `II期` 母框架。

## 当前归位总览

| 桶位 | 数量 | 当前含义 |
|------|------|----------|
| `8` 条公式主链 | `0` | 本轮原始文件没有直接成为公式本体；主链仍由第一轮冻结的抽象公式维持 |
| `L1` 表达层 | `16` | 通用写作与风格表达基础资产 |
| `L2.5` 医学映射层 | `9` | 医学领域对 `L1` 表达资产的映射、叠加与选择表 |
| 证据停放位 | `1` | 合规 / 证据锚定 schema，先停放，不并入正文生成层 |

## 8 条公式主链

本轮没有文件直接入主链本体。  
原因很简单：第一轮冻结的是抽象公式，当前这批原藏经阁文件承担的是表达、映射、选择与证据门禁，不是 `写作目标 / 写作目的 / 写作范围` 这些公式节点本身。

## L1 表达层

| 文件 | 当前归位 | 归位理由 | 与第一轮冻结结果的关系 |
|------|----------|----------|------------------------|
| `expression_base.json` | `L1` 表达层 | 通用转场与禁用表达底座 | 承接“L1 继续承接语域、去 AI 味、表达库存” |
| `native_syntax_rules.json` | `L1` 表达层 | 通用中文句法与去机味戒律 | 只约束正文落句，不改写 `8` 公式主链 |
| `register_levels.json` | `L1` 表达层 | 语域分级系统 | 属于表达力度调节，不是公式节点 |
| `style_dna_schema.json` | `L1` 表达层 | 文风配置结构定义 | 作为风格结构底座，不升格为新理论层 |
| `m1_argument_logic_rules.json` | `L1` 表达层 | 论证门禁资产，支撑推演但不替代主链公式 | 对应首轮里“规则要拆成门禁、组合规则、逻辑规则”的口径 |
| `m2_narrative_primitives.json` | `L1` 表达层 | 叙事原语库 | 对应首轮里“构件先拆件再回填”的口径 |
| `m4_rhetoric_blocks.json` | `L1` 表达层 | 通用修辞积木原型 | 仍是表达积木，不是医学映射层 |
| `persona_kernels.json` | `L1` 表达层 | 通用人格内核 | 为 `人格` 提供素材，但不是主链本体 |
| `lighthouse_banfoxianren.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 作为风格输入，不改变首轮冻结版 |
| `lighthouse_bingdian.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |
| `lighthouse_chengongzi.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |
| `lighthouse_dailaoban.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |
| `lighthouse_kongzhike.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |
| `lighthouse_renwu.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |
| `lighthouse_shenlanguan.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |
| `lighthouse_zhangjiaw.json` | `L1` 表达层 | 作者灯塔画像与风格蒸馏 | 同上，仍是表达库存 |

## L2.5 医学映射层

| 文件 | 当前归位 | 归位理由 | 与第一轮冻结结果的关系 |
|------|----------|----------|------------------------|
| `m0_persona_templates.json` | `L2.5` 医学映射层 | 医学人格叠加模板 | 对应首轮里 `人格` 可继续承接领域叠加，但不回到旧母框架 |
| `m3_strategic_plays.json` | `L2.5` 医学映射层 | 医学战略剧本与叙事序列 | 属于医学场景映射，不是通用理论层 |
| `m4_medical_blocks.json` | `L2.5` 医学映射层 | 医学修辞块及 `M2→M4` 映射 | 直接印证“`M4` 是通用写作规则的医学映射” |
| `medical_expression_profiles.json` | `L2.5` 医学映射层 | 受众画像与映射规则 | 负责把表达姿态映到医学受众 |
| `medical_syntax_rules.json` | `L2.5` 医学映射层 | 医学增量句法 | 是 `L1` 句法的医学叠加，不是新语法理论 |
| `narrative_arcs.json` | `L2.5` 医学映射层 | 医学作者弧与选弧指南 | 负责场景选择与弧线映射 |
| `persona_medical_overlay.json` | `L2.5` 医学映射层 | 从 `L1` 迁移的医学 persona 叠加 | 证明医学 persona 是叠加件，不是独立本体 |
| `rhetoric_blocks.json` | `L2.5` 医学映射层 | 医学作者块与隐喻系统 | 是医学领域的表达映射表 |
| `sentence_patterns.json` | `L2.5` 医学映射层 | 医学作者句式与场景选型 | 是医学领域的句式映射表 |

## 证据停放位

| 文件 | 当前归位 | 归位理由 | 与第一轮冻结结果的关系 |
|------|----------|----------|------------------------|
| `m5_compliance_schema.json` | 证据停放位 | 以证据锚点、验证失败与规则校验为中心，先停放，不并入正文生成层 | 对应第一轮的“证据要入链，但不能把校验 schema 误冻成正文理论” |

## 旧判断 -> 触发证据 -> 新判断 -> 影响范围

| 旧判断 | 触发证据 | 新判断 | 影响范围 |
|--------|----------|--------|----------|
| `L2` 医学资产可以继续按并列能力件散放 | `m4_medical_blocks.json`、`medical_expression_profiles.json`、`narrative_arcs.json`、`sentence_patterns.json` 都在做 `mapping_rules` / `selection_guide` / `overlay` | 统一收成 `L2.5` 医学映射层，不再把它们写成新的理论层 | 影响 `9` 个 `l2/medical_playbook` 文件 |
| `m5_compliance_schema.json` 可以和医学表达层并排 | 该文件 `14` 条规则全部围绕 `evidence anchors`、`validation failure`、`logic` | 先停到证据位，不并入正文生成层 | 仅影响 `1` 个文件 |
| `L1` 基础资产可以被理解为主链本体 | `expression_base.json`、`native_syntax_rules.json`、`register_levels.json`、`style_dna_schema.json` 等内容都在定义表达、语域、句法与风格结构，而不是公式节点 | `L1` 资产继续留在表达层，不直接改写 `8` 条公式主链 | 影响 `16` 个 `l1/writing_craft` 文件 |

## 与第一轮冻结结果的关系

1. 第一轮冻结的是 `8` 条公式主链，本轮没有改写这 `8` 条公式本体。
2. 本轮只是把当前可用文件挂到主链外侧：`L1` 负责表达基础，`L2.5` 负责医学映射，`m5` 负责证据门禁。
3. 因此，第二轮结果是对第一轮冻结版的补充落点，不是新一轮理论重开。

