# IIF-I期中间环节目录说明

状态：Active  
日期：2026-04-05  
用途：收纳 `IIF` 提前终止后，围绕“是否启动 `IIG`，还是重回 `I期`”展开的过渡讨论与阶段决议；本目录按固定少数载体运行，不继续滚雪球式新增独立过程文档。

## 证据摘要

- 已直接核对 [阶段过渡操作规则.md](../../../../03_阶段过渡/00_阶段过渡操作规则.md)，确认当上一阶段结束、下一阶段方案尚未冻结时，应建立新的固定过渡目录。
- 已直接核对 [IIF期状态卡.md](../../../../02_阶段结果/IIF期/00_状态卡.md) 与 [C0_审计整编中心/02_C1C2交叉裁决表.md](../../../../02_阶段结果/IIF期/C0_审计整编中心/02_C1C2交叉裁决表.md)，确认 `IIF` 已完成最小单元审计与 `42 -> 21 + 14 + 7` 的交叉裁决，但未启动 `32` 篇复验。
- 当前线程已明确确认：本轮讨论已不再只是 `IIF` 内部如何继续复验，而是转向“以 `8` 条公式重整写作规范层对象体系”，因此需要新的阶段过渡载体留痕。

## 收纳范围

本目录只收：

1. `IIF` 提前终止后的过渡讨论
2. 是否启动 `IIG` 或重回 `I期` 的阶段判断
3. 围绕 `8` 条公式形成的阶段决议
4. 面向下一阶段的补充执行文档
5. 当前待执行事项

本目录不收：

1. `IIF` 正式审计结果
2. `IIF` 既有 `C0/C1/C2` 提交物
3. 下一阶段冻结版正式实验方案

这些内容仍分别进入：

1. [02_阶段结果/IIF期](../../../../02_阶段结果/IIF期)
2. [01_实验方案](../../../../01_实验方案)

## 当前一句话口径

`IIF` 已提前终止；当前不启动 `IIG`，先进入 `IIF-I期中间环节`，讨论是否按 `8` 条公式重回 `I期` 重整范围内资产。

## 当前固定载体

本目录当前只保留下面 `5` 份活文档：

1. `00_目录说明.md`
2. `01_讨论过程纪要.md`
3. `02_讨论结果与阶段决议.md`
4. `03_补充执行文档.md`
5. `04_待执行事项.md`

