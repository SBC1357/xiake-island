# 03_补充执行文档

状态：Active  
日期：2026-04-05  
用途：收纳 `IIF -> I期` 过渡阶段当前仍需要保留的补充执行信息；本文件同时作为当前唯一推荐的新 session 交接入口。

## 证据摘要

- 已直接核对 [IIF期状态卡.md](../../../../02_阶段结果/IIF期/00_状态卡.md)，确认 `IIF` 已提前终止，现有结果区只保留为证据。
- 已直接核对 [C0_审计整编中心/02_C1C2交叉裁决表.md](../../../../02_阶段结果/IIF期/C0_审计整编中心/02_C1C2交叉裁决表.md)，确认 `42` 条主审计清单的审计结果已收成 `21 + 14 + 7`。
- 已直接核对 [IIF-I期中间环节/02_讨论结果与阶段决议.md](02_讨论结果与阶段决议.md)，确认当前不启动 `IIG`，而是重回 `I期`。

## 一、当前有效执行指针

1. `IIF` 当前结果，只以正式结果区为准：
   - [IIF期](../../../../02_阶段结果/IIF期)
2. 当前有效阶段决议，以本过渡目录为准：
   - [02_讨论结果与阶段决议.md](02_讨论结果与阶段决议.md)
3. 已使用完毕的 `IIF` 复验推进口径，不再作为当前主线执行依据。

## 二、当前唯一主线

当前主线只做一件事：

`按 8 条公式，把范围内资产重新归类并收回主链。`

这里的“范围内资产”只指写作规范层相关资产，不直接包含：

1. `21 + 14 + 7` 这类审计 / 执行分流结果
2. 旧阶段的桥接层、生成层或侠客岛反推层执行件

## 三、当前最硬的执行限制

1. 不准直接启动 `32` 篇复验
2. 不准以旧 `II期` 口径直接立 `IIG`
3. 不准把 `21 + 14 + 7` 继续当成当前主解释框架
4. 不准恢复 `ARCH-01 / ARCH-06` 这类旧框架为当前母口径
5. 不准把“矩阵 / 线性代数”工程表达直接升格为正式理论
6. 允许把 `32` 篇文章当成 `写作目标 / 写作目的` 的样本池做上游推演，但这不允许落成逐篇复验、逐篇记账、逐篇命中统计或逐篇裁决
7. `文章大纲 / 有效大纲 / 写作范围` 的形态定义，只能基于当前已经深分析过的文章，不得调用 `32` 篇全量样本池扩做文章级扫描
8. 第 `5` 项必须全量覆盖 `32` 篇样本，不准抽样、不准跳过、不准只用旧摘要和旧表代替阅读
9. 五项任务每完成一项都必须立刻回写一轮交付文档；没回写，不算完成
10. 每一轮回写至少必须更新 `00_状态卡.md`、当前对应主文档、`05_阶段结论.md`

## 四、给新 Session 的唯一交接口径

新 session 现在不要做三件事：

1. 不要回到 `IIF` 内继续派 `32` 篇复验
2. 不要直接起 `IIG` 正式方案
3. 不要先造新概念再回头收口

新 session 只需要先回答一件事：

`基于 8 条公式，范围内资产应如何重新归类，并以什么顺序回填到新一轮 I期主链。`

补充硬定义：

1. 如果读取 `32` 篇文章，只能为了提取 `写作目标 / 写作目的` 的候选资产池。
2. 任何逐篇表、逐篇卡、逐篇命中统计、逐篇裁决，一律视为偷跑 `IIF` 复验。
3. 如果某项已经在思考或口头上推进，但没有回写到交付文档，一律按“未完成”处理。

