# III期临床当前阶段任务卡

状态：工作版 / 冷启动任务卡
日期：2026-04-10
用途：告诉新 Agent 现在这套 III 期临床已经做到哪、当前唯一主任务是什么、哪些事不要顺手扩做

## 这张卡解决什么

1. 让新 Agent 先知道当前阶段不是“再扩一层新架构”或“继续收启动前准备层”，而是先在扩样结果基础上修 key_facts / 字数控制，不得倒退回旧 blocker。
2. 让新 Agent 先知道当前唯一主任务、当前阻塞和当前下一步。
3. 让新 Agent 先知道哪些结论只是工作版，哪些不能说成已冻结。

## 当前阶段

当前阶段先按下面这句话理解：

`IIIA 扩样 3 case 已跑到明确结果点（patient 77.5 FAIL / mechanism 82.05 PASS / news 字数门禁 FAIL）；其中 2 case 含评分闭合，supplement 链路 3/3 健康；当前处于"key_facts 系统性弱项待修、news 字数门禁待调"阶段，不允许进入 IIIB。`

这里必须明确：

1. patient 与 mechanism 已验证全链（上传→证据→规划→写作→出稿→评分）可复现闭合；news 已跑到 delivery 前的明确失败点（字数门禁）。
2. supplement 链路已有 3 case 直证健康（无上传失败）。
3. 六维归因已定位系统性弱项：`key_facts` 是跨题最弱维度。
4. 当前不是从零搭架构，也不是补模板，而是在扩样基础上修内容质量（写作 prompt 层）和字数控制。

## 当前接手状态

1. 入口四件套已落盘，新 Agent 现在有门可进。
2. 扩样 3 case 已跑到明确结果点：patient 77.5 FAIL / mechanism 82.05 PASS / news 字数门禁 FAIL。
3. 评分六维归因已可读：key_facts 是系统性最弱维度。
4. 单题全貌档案 3/3 已存在，但“有档案”不等于“有推进”；后手必须先看全文、材料、prompt 和 runtime，再谈判断。
5. 结果总览、裁决、回写清单都只算控制件，不能再替代单题全貌和 runtime 直证。
6. supplement 链路已验证健康（3/3 无上传失败）。

## 当前唯一主任务

当前唯一主任务先收成下面 `3` 句：

1. 修写作 prompt 层的 key_facts 覆盖率：在写作指令中显式列出"必须覆盖的关键数字清单"，解决跨题最弱维度。
2. 调字数控制：news 差 30 字被拒，需在 prompt 层或门禁容忍度上微调。
3. 补齐 news 评分：对 news 的 generated.txt 做 score-only rerun，完善统计基线。

## 最近已落盘的硬结果

当前至少已经有下面这些可直接回看的硬结果：

1. `00_入口索引` 四件套已经落盘。
2. `02_节点模块规则明细层/02_稿型识别与路由规则.md` 已回写 `Route-A / B / C / D`、入口条件、摇摆边界和当前杠杆缺口。
3. `D:\\汇度编辑部1\\侠客岛-runtime\\iii_route_validation\\00_状态卡.md`、`02_10题分流定标表.md`、`03_20样稿泛化验证表.md`、`04_路线判定条件表.md`、`06_最近直接验证锚点.md` 已存在，可作为继续建设前的直接证据入口。

## 当前已落盘阻塞

这里只记录当前 III期临床 主线的稳定问题方向。

1. key_facts（关键事实覆盖）是跨题系统性最弱维度：patient 65, mechanism 75，均为最低或次低。
2. hallucination_control 偶发严重失分：patient 60（关键数字错写），mechanism 86（正常），说明当前系统偶尔会编造关键数字。
3. news 字数门禁 FAIL：DOCX 2490 < 2520 下限（差 30 字），字数控制精度不足。
4. 扩样 PASS 率仅 1/3（mechanism），统计基线仍不完整。
5. Vision API 未在本轮做专门独立测试（本轮 3 case 均为 PDF 上传路径）。

## 当前最优先的建设点

如果当前任务仍在 III 期临床主线上，优先顺序先按下面走：

1. 先用单题全貌和 runtime 审稿包回看 patient / mechanism 的真实喂料与真实 prompt，定位 key_facts 丢失点，再改写作 prompt。
2. 调字数控制（prompt 层加强或门禁容忍度微调），解决 news 差 30 字问题。
3. 对 news generated.txt 做 score-only rerun，补齐第 3 个评分数据点。
4. 只有 key_facts 和字数问题都有改善迹象，才允许往 IIIB 走。

## 当前新增硬门

1. 没有 `materials_full.json`、`writing_system_prompt.txt`、`writing_user_prompt.txt`、`generated.txt`、`task_detail.json` 的 case，不算可人工审稿的有效 run。
2. 没有单题全貌里的全文、材料全文、prompt 全文，不得汇报“这题已收口”。
3. 文档更新、裁决快照和回写清单同步，不算阶段推进本身。

## 当前不该顺手做的事

1. 不要重写 `03_完整架构层`。
2. 不要把 `05` 里的模板文件直接写成“已可执行”。
3. 不要让旧 `IIIA期试验` 继续承接当前执行权。
4. 不要为了方便解释再新开一层体系或新造术语。
5. 不要把 runtime 里的过程验证结果直接当正式真相源。

## 已经明确是工作版的内容

下面这些现在都按“工作版 / 待继续补证”处理：

1. `02_节点模块规则明细层` 中的大多数节点规则。
2. 目前已经写出的标准件与标准件预留位。
3. `Route-A / B / C / D` 及其边界判断。
4. 与 `10题 + 20样稿` 相关的路线验证结论。

## 下一棒第一步

如果下一棒是继续建设或继续验证，第一步固定先做下面这条：

1. 先用 patient 和 mechanism 的审稿包逐条回看 `materials_full.json` 与 `writing_*prompt.txt`，定位 key_facts 丢失点；确认后再改写作 prompt 并做重新生成。

只有这一步做完，才允许往下选第二步。

## 当前可直接复用的硬结果

当前可以直接复用，但仍按工作版处理的硬结果，至少包括：

1. 上游固定路由链：`真实写作场景 -> 一级稿型 -> 主二级稿型 -> 主样稿母本`
2. 返修主纪律：每一轮修改都新开 session
3. 冻结层目标：`不丢失 + 降噪 + 不多不少`
4. 当前最小路线集合：`Route-A / Route-B / Route-C / Route-D`
5. `04` 层已经明确：只保留 `IIIA` 锚点，后续阶段数不预设

## 如果你现在要继续建设，先从哪下手

默认只按本卡“当前最优先的建设点”执行，不再另开新排序：

1. 先用 patient / mechanism 的审稿包回看真实喂料与真实 prompt，定位 key_facts 丢失点，再修写作 prompt。
2. 再调字数控制，解决 news 差 30 字问题。
3. 最后补齐 news 的 score-only rerun，完善统计基线。

仍需顺手防守的只有一条真相源纪律：

1. 当前阶段 / 当前阻塞 / 当前下一步只认本卡；阶段包、裁决结论、runtime 留痕都不得再并行承载活状态。

## 证据摘要

1. 已直接核对 `02_节点模块规则明细层/02_稿型识别与路由规则.md`，确认当前已经有 `Route-A / B / C / D` 的工作版路线集合、入口条件和摇摆边界。
2. 已直接核对 `03_完整架构层/01_III期完整架构.md`，确认当前系统主链已经成型，但整体仍标注为“整合稿 / 待继续补证”。
3. 已直接核对 `scripts/run_ii_phase.ps1`、`scripts/run_ii_benchmark.ps1`、`scripts/benchmark_runner.py` 与 `scripts/config/ii_benchmark_cases.json`，确认当前应以 `run_ii_benchmark.ps1` 作为 IIIA 冒烟唯一入口；`run_ii_phase.ps1` 仍是 II 期总控，`benchmark_runner.py` 仍是 3 题硬编码注册表。
4. 已直接核对 `等老刘-P096（学士）` 与 `仑卡奈单抗患者向文章` 目录，确认当前同时存在“平铺目录”和“`inputs/ + gold/` 目录”两种形态，前者当前应 blocked，后者可先冒烟。
5. 旧 smoke 失败日志（环境异常期）落在 `D:\汇度编辑部1\侠客岛-runtime\ii_phase_runs\20260410_004116_832_IIC首批\`。环境已于 2026-04-10 修复：`.venv` 下 `import asyncio` 通过、后端 `/health` 返回 `{"status":"ok"}`。
6. 首枪 smoke 成功：`D:\汇度编辑部1\侠客岛-runtime\ii_phase_runs\20260410_033514_123_IIC首批\`，lecanemab_patient 72.45/100 PASS，全链闭合（2026-04-10 同轮验证）。
7. 评分链修复：`benchmark_runner.ps1` 候选顺序调整，MiniMax-M2.5 升为首位；volcengine-plan 当前 401。
8. 已直接核对 `D:\汇度编辑部1\侠客岛-runtime\score_parse_verify\20260410_fix_dims\lecanemab_patient\score.json` 与同目录 `benchmark_report_latest.json`，当前 `dimensions` 已是 6 项数组，`weighted_total` 为 77.45，说明评分解析修复已生效。
9. 已直接核对 `D:\汇度编辑部1\侠客岛-runtime\ii_phase_runs\20260410_112025_027_IIC首批\run_summary.md` 与 `lecanemab_patient\score.json`，确认二次全链复现为 `77.5/100`，`qualified=false`。
10. 已直接核对 `D:\汇度编辑部1\侠客岛-runtime\ii_phase_runs\20260410_112611_555_IIC首批\run_summary.md` 与 `lecanemab_mechanism\score.json`，确认 mechanism 为 `82.05/100`，`qualified=true`。
11. 已直接核对 `D:\汇度编辑部1\侠客岛-runtime\ii_phase_runs\20260410_115503_908_IIC三题\run_summary.md` 与 `lecanemab_news\task_detail.json`，确认 news 因 `2490 < 2520` 的字数门禁失败，未进入评分阶段。
12. 已直接核对 `scripts/benchmark_runner.ps1`，当前执行链已补落 `materials_full.json`、`writing_system_prompt.txt`、`writing_user_prompt.txt`、`review_bundle.json`，后续 rerun 应以这些产物作为审稿入口。
