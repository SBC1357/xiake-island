# III期临床经验裁决总表

状态：工作稿 / 首轮收敛版  
日期：2026-04-08  
用途：把当前最有价值的散落经验压成“最小抽象 + 节点落点 + 处理状态”，作为 `02_节点模块规则明细层` 的唯一上游  
边界：本表只做裁决，不直接展开成长文规则

## 证据摘要

1. 已直接核对 III 期母图、接口路由、模块补丁、模块卡规则文档，确认现有正式口径已经覆盖输入、路由、合同、门禁、返修与模块层，但仍然散落。
2. 已直接核对冷启动配置、稿型总表和实战经验归档，确认真实生产经验主要落在输入标准化、稿型绑定、写作合同边界、审改 loop、上下文工程和局部回修规则。
3. 已直接核对 `04-08` 两份总结逐字稿，确认近期新增经验重点是：输入预处理目标、大纲独立过闸、上下文粒度优先级、全稿生成与局部修改分流、每轮修改都新开干净 session。
4. 已直接核对仓库级硬约束，确认案例进入系统前必须先经过“来源 -> 抽象 -> 落点 -> 裁决”，不能直接抬成完整架构。

## 一、裁决口径

状态只允许使用下面 `5` 类：

1. `保留`
2. `收紧`
3. `合并`
4. `删除`
5. `待验证`

这里必须再明确：

1. `保留 / 收紧 / 合并` 只表示这条经验允许进入当前 `02` 层工作版，不等于 III 期实验已经最终证明。
2. 只有在 `02` 层长期稳定、且确实影响主链职责或路由，才允许再升到 `03` 层。

## 二、经验条目

| 编号 | 最小抽象 | 主要来源 | 正式落点 | 状态 |
| --- | --- | --- | --- | --- |
| E-01 | 输入标准化的目标不是“整理一下”，而是 `不丢失 + 降噪 + 不多不少` | `04-08` 逐字稿、冷启动配置、接口路由文档 | 输入标准化与冻结规则、完整架构 | 保留 |
| E-02 | 用户输入渠道默认就是对话框与原始材料，不要求用户先做中间整理件 | 冷启动配置、接口路由文档 | 输入标准化与冻结规则、完整架构 | 保留 |
| E-03 | 真实写作场景 -> 一级稿型 -> 主二级稿型 -> 主样稿母本，是上游固定路由链 | 稿型总表、接口路由文档、母图 | 稿型识别与路由规则、完整架构 | 保留 |
| E-04 | 八公式真正有价值的不是“八”本身，而是 `15` 个公式位对资产存储和编排的约束 | `04-08` 逐字稿、现有方法文档 | 八公式与资产编排规则、完整架构 | 保留 |
| E-05 | 八公式在 II 期失败的关键原因之一，是没有完成模块化和递归化 | `04-08` 逐字稿、现有方法文档 | 八公式与资产编排规则、完整架构 | 保留 |
| E-06 | 大纲不是附属物，而是独立过闸点；大纲出来后要快速确认 | `04-08` 逐字稿 | 大纲过闸与写作合同规则、完整架构 | 保留 |
| E-07 | 写作质量最稳的场景是“上下文定死后的局部补写”，不是“从头空写” | `04-08` 逐字稿、改稿复盘 | 首稿生成规则、完整架构 | 保留 |
| E-08 | 当前工作版先按这条上下文优先级使用：`给上文 + 给下文 + 补中间一句` 默认优于 `只给上文接下一句`；只给提纲从头写开头质量最低 | `04-08` 逐字稿 | 首稿生成规则 | 保留 |
| E-09 | 当前工作版先按“开头和结尾天然缺一侧上下文、默认比中段更难稳定”处理 | `04-08` 逐字稿、改稿复盘 | 首稿生成规则、审稿与门禁规则 | 保留 |
| E-10 | AI 写稿、审稿、改稿都要服从同一套“好稿共用约束” | `03_仑卡奈单抗_改稿复盘.md`、模块补丁 | 首稿生成、审稿与门禁、返修 loop | 保留 |
| E-11 | 返修 loop 不是可选项，而是主链；问题必须先分级，再决定回哪一层 | 模块补丁、模块卡规则、`04-08` 逐字稿 | 返修 loop 与 session 纪律、完整架构 | 保留 |
| E-12 | 结构性问题不是“继续在旧会话里修”，而是回更上游重开，并且新开 session | 模块补丁、`04-08` 逐字稿 | 返修 loop 与 session 纪律、完整架构 | 收紧 |
| E-13 | 当前工作版先按“每一轮修改都新开全新 session，只吃本轮标准上下文和干净约束”执行 | `04-08` 逐字稿、模块补丁 | 返修 loop 与 session 纪律 | 保留 |
| E-14 | 当前工作版先把局部修改粒度压到 `一句话 / 一个信息点 / 一个空缺位` | `04-08` 逐字稿、模块补丁 | 返修 loop 与 session 纪律 | 保留 |
| E-15 | 当前工作版默认串改，不默认并改；并发只适用于完全独立的空缺位 | `04-08` 逐字稿、模块补丁、模块卡规则 | 返修 loop 与 session 纪律 | 收紧 |
| E-16 | 当前工作版不让生成全稿 Agent 和局部修改 Agent 共用一套内置 prompt | `04-08` 逐字稿 | 首稿生成规则、返修 loop 与 session 纪律 | 保留 |
| E-17 | 情绪、逻辑、语气等审稿标准可以量化，但前提是有足够案例支撑锚点 | `04-08` 逐字稿 | 审稿与门禁规则 | 待验证 |
| E-18 | 旧会话历史偏差会放大返修横跳，因此旧 session 只留作原始证据，不继续执行修改 | 模块补丁、`04-08` 逐字稿 | 返修 loop 与 session 纪律 | 保留 |
| E-19 | 案例层不能直接抬成架构层，必须先经过来源 -> 抽象 -> 落点 -> 裁决 | 母图、仓库级规则 | 散落经验裁决层、完整架构 | 保留 |
| E-20 | 反向确认不是礼貌动作，而是防止 Agent 擅自补意思后误开工的硬闸门 | 仓库卫生最高禁令 | 需求对齐回执规则、完整架构 | 保留 |
| E-21 | 先判稿型，不判不写；很多错误不是句子差，而是把不同稿型混着写了 | 稿型总表、冷启动配置、实战案例 | 稿型识别与路由规则、大纲过闸与写作合同规则 | 保留 |
| E-22 | 样稿或目标写法没全文直读前，不得宣称“已对齐” | 冷启动配置、实战案例 | 需求对齐回执规则、输入标准化与冻结规则 | 保留 |
| E-23 | 第一旁证层必须任务化，不能默认套通用旁证模板 | 实战案例、逐字稿 | 大纲过闸与写作合同规则 | 保留 |
| E-24 | 开头允许有钩子，但前两段内必须尽快入题，不能长背景后入题 | 实战案例 | 大纲过闸与写作合同规则、审稿与门禁规则 | 保留 |
| E-25 | “解释阈值”和“去元话术”应并成一条规则：只补必要人话解释，禁止解释自己为什么这样写 | 实战案例 | 大纲过闸与写作合同规则、审稿与门禁规则 | 合并 |
| E-26 | 研究段必须保留最小动作链，指标到结论之间必须有证据桥 | 实战案例、改稿复盘 | 大纲过闸与写作合同规则、审稿与门禁规则、模块层规则明细 | 保留 |
| E-27 | 价值判断必须从证据链自然长出，不能只讲边界，也不能最后硬拔高 | 实战案例、改稿复盘 | 大纲过闸与写作合同规则、审稿与门禁规则 | 保留 |
| E-28 | 用户明确认可的开头句，默认升格为语气锚点和保留区 | 实战案例 | 返修 loop 与 session 纪律 | 保留 |
| E-29 | 横向比较必须先标证据类型，再谈强弱；不能跨维度硬比 | 实战案例 | 审稿与门禁规则 | 保留 |
| E-30 | 局部论点不得擅自改写学科级定义，也不能为了成文强行一因统摄 | 实战案例 | 审稿与门禁规则 | 保留 |
| E-31 | 未完全对齐前，优先出内容卡、对比卡、证据卡，不早写 prose | 实战案例 | 大纲过闸与写作合同规则 | 保留 |
| E-32 | 案例先积累，再反抽清单；单案例不直接扩规则 | 实战案例、仓库级规则 | 经验回流规则、完整架构 | 保留 |
| E-33 | 竞争性医学写作的批判力来自事实悖论和硬数据，不来自修辞猛冲 | 实战案例 | 稿型识别与路由规则、审稿与门禁规则 | 收紧 |
| E-34 | 只要已有等价可读材料，就应立刻切回材料，不继续折腾工具 | 生产接入经验、实战案例 | 输入标准化与冻结规则 | 待验证 |

## 三、当前最值得先升到 02 层的条目

优先级最高的 `12` 条：

1. `E-01` 输入标准化目标
2. `E-03` 稿型路由链
3. `E-04` / `E-05` 八公式与公式位边界
4. `E-06` 大纲独立过闸
5. `E-07` / `E-08` 上下文工程优先级
6. `E-13` / `E-14` / `E-15` session 纪律与局部修改粒度
7. `E-21` 稿型先判
8. `E-22` 样稿未直读不宣称对齐
9. `E-23` / `E-31` 先卡片化再 prose
10. `E-24` / `E-25` 开头纪律与去元话术
11. `E-26` / `E-27` 动作链、证据桥和值判断边界
12. `E-32` 经验先积累再反抽

## 四、当前不建议直接升到 03 层的条目

1. `E-08` / `E-09` / `E-14` / `E-15` / `E-16` 这类局部生成与返修微操  
原因：已经适合进入 02 层执行规则，但不该把句位级和局部粒度细节抬成架构主叙述。

2. `E-17` 情绪/逻辑/语气量化  
原因：方向清楚，但锚点案例和量化区间还不够稳。

3. `E-24` 到 `E-31` 这类稿型专用或句位级写法边界  
原因：应该主要留在 02 层规则明细，不直接让 03 层膨胀。

4. 任何仍然只存在于单次逐字稿、尚无配套案例支撑的局部想法  
原因：适合先进 `待验证经验池`，不直接写成完整架构。
