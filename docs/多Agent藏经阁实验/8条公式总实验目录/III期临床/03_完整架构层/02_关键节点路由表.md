# III期关键节点路由表

状态：整合稿 / 待继续补证  
日期：2026-04-11  
用途：把 III 期关键节点的输入、输出、过闸条件和回流方向压成一张白盒路由表  
边界：本文件是架构附表，不展开案例层全文

## 证据摘要

1. 已直接核对现有 III 期母图、接口路由和模块补丁，确认当前主链节点已经具备较稳定的顺序和职责。
2. 已直接核对近期逐字稿，确认近期最大的新增在于：大纲过闸、全稿/局部修改分离、返修 loop 分级和经验回流独立化。
3. 已直接核对本轮 IIIA 运行直证与白盒规划，确认上游必须补出 `主题探索与扩证层`、`正式主题确认与锁策略层` 两个显式节点。
4. 已直接核对 fixed-baseline revise 与 loop+local revise 直证，确认定向返修层当前至少要区分 `whole revise` / `local revise`，且 local 分支的首要成功判据不是总分，而是执行精度。

| 节点 | 上游输入 | 节点动作 | 下游输出 | 过闸条件 | 失败时回流 |
| --- | --- | --- | --- | --- | --- |
| 任务进入池 | 用户原始输入 | 全量接收任务、材料、样稿、修改意见 | 原始任务包 | 输入未明显冲突 | 无 |
| 需求对齐回执层 | 原始任务包 | 锁真实写作场景、一级稿型、主二级稿型、主样稿母本、交付物、篇幅与窗口；判断当前是正式命题还是候选态 | 极短需求对齐回执 | 用户纠偏已吃进，任务理解稳定；未决项和最小补问已显式化 | 留在回执层纠偏 |
| 输入标准化与冻结层 | 已对齐任务包 | 做 `不丢失 + 降噪 + 不多不少` 的预处理，形成探索前可稳定复用的正式输入 | 冻结输入包 + 主题探索前置卡 | 主问题源、禁区、样稿直读状态、扩证前置条件可稳定复用 | 回需求对齐层 |
| 稿型识别与资产路由层 | 冻结输入 | 锁稿型路线、样稿母本、样稿借用边界和篇幅级别 | 路由结果 + 样稿合同 + 资产组合 | 稿型、样稿路线和篇幅级别稳定 | 回需求对齐 / 冻结层 |
| 主题探索与扩证层 | 路由结果 + 样稿合同 + 冻结输入 | 形成候选主题、扩证、组织逻辑链/故事链，给出推荐主线与备选主线 | 候选主题包 + 推荐/备选主线卡 | 主线、关键支撑点、大致结论/落点已成链 | 留在本层继续扩证 |
| 正式主题确认与锁策略层 | 候选主题包 + 推荐/备选主线卡 | 确认正式主题、主题锁策略，并判断当前是否已达到成文闭环 | 正式主题卡 + `theme_lock_policy` + 成文闭环判断 | 正式主题成立；是否允许后续改题已写明；当前可否进入写作已判清 | 回主题探索层 |
| 大纲过闸与写作合同层 | 正式主题卡 + `theme_lock_policy` + 资产组合 | 生成并审核大纲，收束段落任务和写作硬约束 | 过闸大纲 + 写作合同 | 故事线、逻辑大纲、主架构、段落任务和禁止动作成立 | 留在本层重开 |
| 首稿生成层 | 过闸大纲 + 写作合同 + 取材包 | 生成首稿 | 首稿 | 首稿能进入门禁审查 | 回大纲层 |
| 审稿与门禁层 | 首稿 + 写作合同 | 产出问题单、评分回指和问题归因；benchmark 模式下可额外挂 `reference compare` 评分包 | 问题单 + 分级结论 + 回指结果 + （benchmark 可选）评分包 | 问题单足够驱动下游修改，且能回指到具体节点；benchmark comparator 不替代问题单 | 继续补门禁判定 |
| 定向返修层 | 问题单 + 原文范围 + 本轮标准上下文 | 按问题级别分流到 `local revise` / `whole revise` / 上游回流 | 修订稿 | `local revise` 先看执行精度、问题关闭率、越界与新风险；`whole revise` 看结构性问题是否收束 | 局部问题留本层；结构问题回合同层；主题问题回主题确认层；任务理解问题回回执层 |
| 经验回流层 | 修订稿 + 问题单 + 人审判断 | 抽取稳定经验，先做裁决，再决定是否升规则 | 经验条目 / 待验证条目 | 最小抽象和落点明确 | 留在经验裁决层 |
