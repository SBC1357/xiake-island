# IIIA期试运行计划

状态：历史封存 / 旧 IIIA 试运行计划
日期：2026-04-10
用途：保留旧 IIIA 首轮 smoke 的历史执行边界和当时约束；不再承担当前活入口或当前执行权。

## 历史准备清单（封存）

1. 当时唯一入口脚本：`scripts/run_ii_benchmark.ps1`
2. 唯一支持的 case 目录形态：题目目录下必须同时存在 `inputs/` 与 `gold/` 子目录；`inputs/` 中至少 `1` 个 PDF，`gold/` 中至少 `1` 个 docx
3. 当时允许先冒烟的 case 集：
   - `lecanemab_patient`
   - `lecanemab_mechanism`
   - `lecanemab_news`
4. 当时唯一输出根：`侠客岛-runtime/ii_phase_runs`
5. 当时唯一评分执行器：`scripts/benchmark_runner.ps1`，当时六维度评分口径由 `run_ii_benchmark.ps1` 直接调用
6. 环境前置：
   - `http://127.0.0.1:8000/health` 返回 `{"status":"ok","service":"xiakedao"}`
   - `OPENAI_BASE_URL` 使用 `https://coding.dashscope.aliyuncs.com/v1`
   - 评分用 LLM key 已在实际执行 shell 中可见，或启动时显式传入 `-ScoringApiKey`
7. 停闸条件：
   - 唯一入口脚本、唯一输出根、唯一评分器任一摇摆
   - 冒烟 case 不满足目录形态
   - `/health` 不通
   - LLM 评分环境缺失
8. 明确 blocked 的题型/目录形态：
   - 当前仍是平铺目录、没有 `inputs/` / `gold/` 子目录的题
   - 当前 `ii_benchmark_cases.json` 已登记、但目录不满足 runner 要求的题

## 环境使用方式（历史）

1. IIIA 不再单独解释整套环境边界。
2. 执行环境统一回指：
   - `04_III期总实验方案层/04_III期执行环境契约.md`
3. IIIA 在本文件里只补本阶段额外前置：
   - 当前唯一预检入口
   - 当前唯一 smoke 入口
   - 当前允许先冒烟的 case 集
   - 当前 blocked 的目录形态
4. 当时所有 Agent 开跑前，先跑：
   - `scripts/iiia/iiia_preflight.ps1`
5. 如果评分 key 在当前 shell 不可见，可显式传：
   - `-ScoringApiKey`
6. 不准再把 `opencode.json`、`.codex\config.toml`、仓库 `.env` 混成一套解释。

## 当时冒烟边界（历史）

1. 首枪阶段仍只认单题 smoke 已完成，不直接跳到 10 题整套。
2. 有效 run 除原有产物外，必须同时落 `materials_full.json`、`writing_system_prompt.txt`、`writing_user_prompt.txt`、`review_bundle.json`；缺任一项，只算链路运行，不算可人工审稿结果。
3. 当前优先级、当前 blocker、当前下一步现已不认本文件；当前活状态统一回到 `00_入口索引/03_当前阶段任务卡.md`。
4. `run_ii_phase.ps1` 当前只算 II 期总控，不作为 IIIA 冒烟唯一入口。
5. `benchmark_runner.py` 当前仍内置 3 题硬编码注册表，不作为当前唯一入口，也不作为当前唯一评分执行器。

## 本轮试运行对象

| 样题/样稿 | 选择原因 | 想验证什么 | 备注 |
| --- | --- | --- | --- |
| `lecanemab_patient` | 当前目录结构同时满足 `inputs/ + gold/`，且已在 runner 支持集合内 | 验证唯一入口、唯一输出根、评分链和单题全貌档案能否串起来 | 本轮默认首个 smoke case |

## 本轮固定观察点

1. 稿子本体是否可读
2. 过程证据是否足够回看
3. 机器评分是否有参考价值
4. 主执行session是否能给出稳定裁决

## 本轮固定输出

1. 单题全貌模板激活规则
2. 唯一入口与唯一输出位
3. 旧执行记录迁移边界
4. blocked 目录形态清单
5. 可人工审稿的 runtime 审稿包产物

## 证据摘要

1. 已直接核对 `scripts/run_ii_benchmark.ps1`，确认它已接 `ii_benchmark_cases.json`、支持 suite / case_id 选择，并自带 `/health` 检查与结构化输出目录；同时确认它实际调用的是 `scripts/benchmark_runner.ps1`。
2. 已直接核对 `scripts/run_ii_phase.ps1`，确认它当前仍是 II 期总控，默认计划还是 `IIC首批 -> 人工 gate`，不适合作为 IIIA 当前唯一入口。
3. 已直接核对 `scripts/benchmark_runner.py`，确认它仍内置 3 题注册表，不适合直接承担当前 10 题配置下的唯一入口职责，也不应被写成当前 smoke 链的唯一评分执行器。
4. 已直接核对 `等老刘-P096（学士）` 与 `仑卡奈单抗患者向文章` 两类目录，确认当前同时存在“平铺目录”和“`inputs/ + gold/` 目录”两种形态；前者当前应 blocked，后者可先冒烟。
5. 已直接核对 `src\adapters\llm_gateway\openai_provider.py` 与 `provider_factory.py`，确认后端写作阶段只认仓库 `.env` 中的 `OPENAI_BASE_URL / OPENAI_API_KEY`，并固定走 `chat/completions` 路径。
6. 已直接核对 `04_III期总实验方案层/04_III期执行环境契约.md`，确认环境总边界已上提到 III 期总层，本文件不再重写。
7. 已直接新增并核对 `scripts\iiia\iiia_preflight.ps1` 的 `.env` 自动加载逻辑；现在预检脚本与正式 runner 使用同一套仓库 `.env` 注入方式，不再要求 Agent 自己手工对齐 shell 环境。
8. 已直接核对 `侠客岛-runtime/score_parse_verify/20260410_fix_dims/lecanemab_patient/score.json` 与同目录 `benchmark_report_latest.json`，当前 `dimensions` 已是 6 项数组，`weighted_total` 为 77.45；因此本文件只保留执行边界，不再并行承载当前优先级。
9. 已直接核对 `scripts/benchmark_runner.ps1`，当前 runner 已开始额外落 `materials_full.json`、`writing_system_prompt.txt`、`writing_user_prompt.txt`、`review_bundle.json`，可作为本文件“有效 run”硬门的直接依据。
