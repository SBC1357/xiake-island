# IIIA期历史回写清单

状态：历史回写控制件 / 非当前活状态真相源
日期：2026-04-10（扩样更新，历史口径冻结）
用途：仅回指历史回写证据，不承接当前活状态；当前活状态只认 `00_入口索引/03_当前阶段任务卡.md` 与 `07_白盒施工包/04_第四轮白盒测试+建设/白盒系统第四轮收口长线程包.md`

硬边界：本文件是历史回写控制件，不是接手第一读物，也不是当前活状态播报源。没有先读单题全貌全文与 runtime 审稿包，不得先看本清单下结论；凡涉及当前阶段、当前阻塞、当前下一步，一律回指上面的活状态真相源。

## 回写原则

1. 只回写本轮有直接证据支撑的发现，不回写推测性结论。
2. 扩样 3 case 已具备交叉印证基础，但仍需 news 结果确认后统一裁决。
3. 每条回写标注证据来源。

## 允许回写到 `01` 层的

- [x] DashScope coding plan 端点：`coding.dashscope.aliyuncs.com/v1`（非 compatible-mode），证据：直接 API 验证 + 成功生稿
- [x] `_overlapped` / WinError 10106 环境修复方法：`.venv` 重建后恢复，证据：同轮 import 验证
- [ ] Vision API key 配置规范（待 Vision 修复后回写）

## 允许回写到 `02` 层的

- [x] 评分 provider 优先级：MiniMax-M2.5 > volcengine-plan（后者当前 401），证据：4 个 provider 逐一验证
- [x] benchmark_runner.ps1 评分候选顺序已修正，证据：`Get-PreferredScoringConfig` 候选列表调整
- [x] 评分维度解析修复（已验证），证据：`侠客岛-runtime\score_parse_verify\20260410_fix_dims\lecanemab_patient\score.json` 与同目录 `benchmark_report_latest.json` 已输出 6 维 `dimensions`
- [x] supplement 链路健康：3 case 均无上传失败（supplement_upload_failures=[]），patient fact=48(catalog=17,supplement=31), mechanism fact=333(catalog=2,supplement=331)；证据：各 case 的 task_detail.json

## 允许回写到 `03` 层的

- 本轮仍无。扩样结果足以支撑阶段包内的工程判断，但尚不足以直接回写 `03` 层正式架构结论。

## 只留在阶段包和 runtime 的

- [x] 首枪 smoke 原始输出：`侠客岛-runtime/ii_phase_runs/20260410_033514_123_IIC首批/`
- [x] 首枪 raw 评分详情：`ii_phase_runs/20260410_033514_123_IIC首批/lecanemab_patient/score.json`（72.45/100 PASS）
- [x] 评分解析复核详情：`score_parse_verify/20260410_fix_dims/lecanemab_patient/score.json`（77.45/100，6 维可读）
- [x] preflight 历史：`侠客岛-runtime/iiia_preflight/` 下多次记录
- [x] 旧 smoke 失败日志：`20260410_004116_832_IIC首批/`（环境异常期）
- [x] 二次复现: `ii_phase_runs/20260410_112025_027_IIC首批/lecanemab_patient/`（77.5/100 FAIL）
- [x] mechanism: `ii_phase_runs/20260410_112611_555_IIC首批/lecanemab_mechanism/`（82.05/100 PASS）
- [x] news: `ii_phase_runs/20260410_115503_908_IIC三题/lecanemab_news/`（字数门禁 FAIL，DOCX 2490 < 2520）
- [x] 单题全貌档案：`06_单题全貌档案/01_lecanemab_patient.md`、`02_lecanemab_mechanism.md`、`03_lecanemab_news.md`

## 汇报前最小核对

1. 当前阶段 / 当前阻塞 / 当前下一步只认 `00_入口索引/03_当前阶段任务卡.md` 与 `07_白盒施工包/04_第四轮白盒测试+建设/白盒系统第四轮收口长线程包.md`，不要让本清单代替活状态播报。
2. 如果本轮新增了 rerun、修复后直证或分数变化，先同步 `06_单题全貌档案/` 与本清单，再做成果汇报。
3. 首枪原始结果和后续复核结果必须显式分开标注，不能把 `72.45` 与 `77.45` 混写成同一次评分。
4. 汇报前交叉核对 `00_阶段入口.md`、`01_阶段目标.md`、`05_试运行结果总览.md`、`07_主执行session裁决结论.md`、本清单与任务卡是否仍在同一口径；只要还有“未激活 / 已激活”并存，先冻结，不汇报。
5. 阶段是否算过只回指 `04_III期总实验方案层/03_阶段验收总表.md`；单题跑通、单题得分或单题 rerun 都不等于阶段通过。
6. 单题全貌档案必须包含至少一版生成稿全文；只留 `generated.txt` 路径、不贴正文，按未收口处理。
7. 单题全貌档案必须让人工无需点路径即可开始审稿，至少包含：`人工审核先看`、`本轮真正喂给写手的材料（人工整理版）`、`本轮给模型的 prompt 全文`、`当前稿件全文`、`当前审核意见与返修抓手`。
8. `materials` 不允许只写 `materials_full.json` 路径；必须把实际喂料整理成人能读的主干，并明确写出 OCR / 页级碎片 / Accepted manuscript 等噪声与缺项。
9. `prompt` 不允许只写指针；必须把 `system prompt` 与 `user prompt` 全文直接贴进单题全貌。没有独立阶段 prompt 的，必须明确写“本轮不存在 + 原因”。
10. 只要本轮 runtime 已存在 `score.json / summary.json`，单题全貌档案就必须同步写出 `评分结果` 与 `当前审核意见与返修抓手`：总分、达标结论、评分状态、各维度关键失分、至少 3 条返修动作；只写路径不算收口。
11. 汇报前必须运行 `python scripts/validate_case_fullview.py docs\\多Agent藏经阁实验\\8条公式总实验目录\\III期临床\\05_阶段实验包\\IIIA\\06_单题全貌档案`；校验不过，不得汇报“单题全貌已齐”。

## 证据摘要

1. 首枪: `20260410_033514_123_IIC首批\lecanemab_patient\score.json` → 72.45/100 PASS。
2. 二次复现: `20260410_112025_027_IIC首批\lecanemab_patient\score.json` → 77.5/100 FAIL（qualified=false）。
3. mechanism: `20260410_112611_555_IIC首批\lecanemab_mechanism\score.json` → 82.05/100 PASS（qualified=true）。
4. news: task_id=c5018ce5-c305-4a43-936c-5f51c4293a02, DOCX 2490字门禁 FAIL（差 30 字）。
5. supplement 健康: 3 case（patient/mechanism/news）均上传成功，无 supplement_upload_failures。news 的 4 PDF 共 621 条 supplement。
6. 字数门禁发现: news 的 DOCX 2490 字距下限 2520 仅差 30 字，quality 门禁已过，delivery 字数门禁卡住。
7. 活状态只认 `00_入口索引/03_当前阶段任务卡.md` 与 `07_白盒施工包/04_第四轮白盒测试+建设/白盒系统第四轮收口长线程包.md`。
