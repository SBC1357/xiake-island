# 单题全貌模板

状态：模板
日期：2026-04-11
用途：一题一档，记录当前同轮 runtime 直证、正式稿件、评分结果与 blocker；人工审稿优先看这里，不看摘要。

## 当前有效口径

1. 后续阶段沿用与 IIIA 相同的展示层硬门：runtime 直证、完稿全文、materials/prompt 指针、评分结果、本轮真实 blocker。
2. 如果本轮已有 `score.json / summary.json`，必须把总分、达标结论、评分状态和各维度写进本文件；只贴路径不算完成。
3. 如果某项本轮不存在，必须明确写“本轮不存在 + 原因”，不得静默跳过。
4. 汇报前建议运行：`python scripts/validate_case_fullview.py <本文件或目录>`

case_id: ``
task_id: ``
run_dir: ``

## 当前本轮结论

1. 

## Runtime 指针

1. `run_summary.md`：
2. `task_detail.json`：
3. `generated.txt`：
4. `materials_full.json`：
5. `review_bundle.json`：
6. `score.json`：
7. `summary.json`：
8. `writing_system_prompt.txt`：
9. `writing_user_prompt.txt`：

## 本轮关键证据

1. 上传与 evidence：
2. 阶段到达情况：
3. 交付与评分：

## materials 指针

1. 当前人工审稿材料文件：
2. 同轮回退锚点：

## prompt 指针

1. system prompt：
2. user prompt：
3. 同轮回退锚点：

## 评分结果

1. 总分：
2. 达标结论：
3. 评分状态：
4. 各维度：
   - `任务完成度`：
   - `关键事实与关键数字覆盖`：
   - `受众匹配与文风匹配`：
   - `AI味儿控制`：
   - `结构与信息取舍`：
   - `标题角度与稿型适配`：
   - `幻觉与越界编造控制`：
5. 如本轮未执行评分，必须明确写：`本轮未执行评分；原因：...`

## 本轮真实 blocker

1. 

## 完稿全文

1. 生成稿全文（必须直接贴入，不得只写路径）：

## 证据摘要

1. 已直接核对：
2. 已直接核对：
