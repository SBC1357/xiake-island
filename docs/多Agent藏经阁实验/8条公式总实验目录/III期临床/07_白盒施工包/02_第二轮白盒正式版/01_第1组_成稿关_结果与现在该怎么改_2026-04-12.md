# 第 1 组：成稿关，结果与现在该怎么改（2026-04-12）

- 这份文件现在只看 `成稿关`，不再把后面 5 个模块混进来。
- 这轮成稿关是有结果的，不是“没跑出来”。
- 全文入口在这里：
  - [03_第1组_仑卡奈单抗患者向文章_正文全文入口_2026-04-12.md](<03_第1组_仑卡奈单抗患者向文章_正文全文入口_2026-04-12.md>)

## 先给一句话结论

1. 成稿关当前不要替换基线稿。
2. 基线稿 `78 分` 仍是当前赢家。
3. 三条新成稿里，`route1` 和 `route2` 当前打平，都是 `68 分`；`route3` 掉到 `58 分`。
4. 你现在在成稿关该做的，不是去改局部补句问题单；而是先决定：
   - 继续保留基线稿
   - 还是要从 `route1 / route2` 里挑一条继续打磨

## 这是不是你现在该干的事

不是旧文档里那 6 条“补 72% / 改 32% / 改术语”的局部问题单直接全接手。

你现在在 `成稿关` 的真正动作应该是：

1. 先比 4 篇整稿：
   - [基线稿](<assets/patient_baseline_draft.md>)
   - [route1 成稿](<assets/patient_draft_route1.md>)
   - [route2 成稿](<assets/patient_draft_route2.md>)
   - [route3 成稿](<assets/patient_draft_route3.md>)

2. 先决定成稿关当前保留谁。
   - 直接证据下，当前答案是：`先保留基线稿`

3. 如果你非要继续打新稿，只看 `route1` 和 `route2`。
   - `route3` 当前先不值得你花时间。

4. 路径判定、局部任务拆分，不是成稿关负责。
   - 那是后面的 `小修派单关` 才负责的事。

## 这轮真结果

### 基线稿

- 分数：`78`
- 状态：`达到门槛`
- 当前角色：对外默认稿
- 直接看稿：
  - [基线稿正文](<assets/patient_baseline_draft.md>)
  - [基线稿完整提示词](<assets/patient_baseline_draft_prompt.md>)
  - [基线稿评分 JSON](<assets/patient_baseline_score.json>)
  - [基线稿评分摘要](<assets/patient_baseline_score_summary.md>)

### route1

- 分数：`68`
- 状态：`未达线`
- 当前判断：比基线弱，不建议直接替换
- 直接看稿：
  - [route1 正文](<assets/patient_draft_route1.md>)
  - [route1 完整提示词](<assets/patient_draft_route1_prompt.md>)
  - [route1 评分 JSON](<assets/patient_draft_route1_score.json>)
  - [route1 评分摘要](<assets/patient_draft_route1_score_summary.md>)

### route2

- 分数：`68`
- 状态：`未达线`
- 当前判断：和 route1 打平，但不是赢家
- 直接看稿：
  - [route2 正文](<assets/patient_draft_route2.md>)
  - [route2 完整提示词](<assets/patient_draft_route2_prompt.md>)
  - [route2 评分 JSON](<assets/patient_draft_route2_score.json>)
  - [route2 评分摘要](<assets/patient_draft_route2_score_summary.md>)

### route3

- 分数：`58`
- 状态：`未达线`
- 当前判断：当前明显掉队
- 直接看稿：
  - [route3 正文](<assets/patient_draft_route3.md>)
  - [route3 完整提示词](<assets/patient_draft_route3_prompt.md>)
  - [route3 评分 JSON](<assets/patient_draft_route3_score.json>)
  - [route3 评分摘要](<assets/patient_draft_route3_score_summary.md>)

## 每条新稿具体卡在哪

### route1 卡点

1. `72% 极早期患者 4 年认知稳定` 这条关键亮点完全缺失。
2. `55%` 的进展风险降低被写成了 `32%`，属于关键事实错误。
3. 额外给了“杂合子 / 非携带者差异化监测建议”，这在当前材料里没有直接支撑。

### route2 卡点

1. 把 `APOE 非携带者 / 杂合子` 的获益泛化成一般患者获益，人群边界模糊。
2. 没有把“临床数字 -> 患者能理解的意义”这道解释桥真正写出来。
3. “人群分流 / 选择建议”被写偏成医学检测建议，没有落回患者决策支持。

### route3 卡点

1. 主线断了，没有形成“焦虑 -> 数据 -> 理解”的承重路径。
2. `72%` 关键亮点完全缺失。
3. `55%` 又被替换成 `32%`，而且数据来源没标清。
4. 术语密度太高，患者稿写成了术语稿。
5. 开场没有建立患者真实焦虑场景。

## 我对成稿关的判断

1. 这轮成稿关的赢家仍然是基线稿。
2. 如果你还要从新稿里挑一篇继续看，先并排看 `route1` 和 `route2`，不要只看一篇。
3. 如果你只问“这一关下一步最值得改哪条”，我会说：
   - `route1` 先修硬事实错误
   - `route2` 先修解释桥和患者决策落点
   - `route3` 当前先放下

## 证据摘要

- 已直接核对 [patient_baseline_score.json](<assets/patient_baseline_score.json>)，基线稿当前是 `78 分`。
- 已直接核对 [patient_draft_route1_score.json](<assets/patient_draft_route1_score.json>)、[patient_draft_route2_score.json](<assets/patient_draft_route2_score.json>)、[patient_draft_route3_score.json](<assets/patient_draft_route3_score.json>)，三条新稿当前分数是 `68 / 68 / 58`。
- 已直接核对 [patient_draft_route1_score_summary.md](<assets/patient_draft_route1_score_summary.md>)、[patient_draft_route2_score_summary.md](<assets/patient_draft_route2_score_summary.md>)、[patient_draft_route3_score_summary.md](<assets/patient_draft_route3_score_summary.md>)，上面的卡点都来自这些直接评分摘要。
