# 第 1 组：审稿打分关，结果与问题单（2026-04-12）

- 这份文件现在只看 `审稿打分关`，不再把别的模块混进来。
- 这一关已经有结果，不是“没跑出来”。
- 这一关负责的是：给成稿打分、判定是否过线、把问题回指到合同层和公式层。
- 这一关**不负责**把问题分流成“局部改句 / 结构返修 / 小修任务”。
  - 路径判定归后面的 `小修派单关（local_task_router）`。

## 这一关到底干什么

1. 先判硬层。
   - 路线对不对。
   - 关键事实有没有漏。
   - 有没有越界或编造。

2. 再判软层。
   - 像不像成熟可发稿。
   - 读起来顺不顺。
   - 是否真贴合患者和家属。

3. 最后把问题写成可追责的回指。
   - 回指到 `writing_contract / formula_contract / materials_coverage` 这些层。
   - 而不是只写一句“稿子一般”。

## 先给一句话结论

1. 在 `patient` 这一题上，审稿打分关三条 route 当前都过线了。
2. 三条 route 当前总分都是 `78`，`qualified=true`。
3. 但如果你现在只先看一条，先看 `route1`。
   - 原因：`route1` 的软层总评当前是 `82`，高于 `route2 / route3` 的 `72 / 72`。
4. 这说明：
   - `review_score` 这一关不是没结果。
   - 但它的结论是“哪篇稿更稳、问题落在哪”，不是替你决定后面走哪条 patch 路径。

## 这轮真结果

### route1

- 总分：`78`
- 是否过线：`true`
- 软层总评：`82`
- 当前判断：三条里最值得先看
- 你直接看：
  - [route1 审稿完整提示词](<assets/patient_review_score_route1_prompt.md>)
  - [route1 评分 JSON](<assets/patient_review_score_route1_score.json>)
  - [route1 评分摘要](<assets/patient_review_score_route1_score_summary.md>)
  - [route1 运行 manifest](<assets/patient_review_score_route1_manifest.json>)
  - [route1 对照参考稿](<assets/patient_review_score_route1_reference_answer.md>)

### route2

- 总分：`78`
- 是否过线：`true`
- 软层总评：`72`
- 当前判断：能过线，但成稿观感判断弱于 route1
- 你直接看：
  - [route2 审稿完整提示词](<assets/patient_review_score_route2_prompt.md>)
  - [route2 评分 JSON](<assets/patient_review_score_route2_score.json>)
  - [route2 评分摘要](<assets/patient_review_score_route2_score_summary.md>)
  - [route2 运行 manifest](<assets/patient_review_score_route2_manifest.json>)
  - [route2 对照参考稿](<assets/patient_review_score_route2_reference_answer.md>)

### route3

- 总分：`78`
- 是否过线：`true`
- 软层总评：`72`
- 当前判断：在这一题上也过线，但当前更像“问题拆解器”而不是“最稳审稿人”
- 你直接看：
  - [route3 审稿完整提示词](<assets/patient_review_score_route3_prompt.md>)
  - [route3 评分 JSON](<assets/patient_review_score_route3_score.json>)
  - [route3 评分摘要](<assets/patient_review_score_route3_score_summary.md>)
  - [route3 运行 manifest](<assets/patient_review_score_route3_manifest.json>)
  - [route3 对照参考稿](<assets/patient_review_score_route3_reference_answer.md>)

## 你现在最该看什么

1. 如果你现在要继续改稿，不要先把三条 route 的所有评分项都抄进正文。
2. 先看 `route1` 的：
   - `missing_or_misaligned`
   - `backtrace`
   - `next_actions`
3. 这是因为 `route1` 当前最像“稳审 + 可发性检查”。
4. `route2` 和 `route3` 也有价值，但更适合拿来对照它们各自的审稿风格，不适合作为你当前唯一改稿依据。

## 三条 route 的审稿风格差在哪

### route1

- 更像门禁判官。
- 先问“硬层能不能过”，再看软层观感。
- 当前更适合拿来做“先定过线，再看能不能发”的判断。

### route2

- 更像把硬层和软层拆开说的两层审稿员。
- 当前更适合你看“为什么能过线，但还不像成熟稿”。

### route3

- 更像问题单生成器。
- 当前更适合你看“问题最后是怎么被拆成下游可执行项”的方向。
- 但这不等于它负责路径分流；真正路径分流仍然是 `local_task_router`。

## 我对这一关的判断

1. `review_score` 在 `patient` 这题上已经不是阻塞点。
2. 当前这关最稳的默认阅读口径是：
   - 先看 `route1`
   - 再拿 `route2 / route3` 看风格差异
3. 如果你只问“这一关有没有把问题真正交付出来”，答案是有。
   - 因为 `score.json` 里已经不只是分数，还包含：
   - `formula_trace`
   - `layer_judgment`
   - `missing_or_misaligned`
   - `backtrace`
   - `next_actions`

## 证据摘要

- 已直接核对 [patient_review_score_route1_score.json](<assets/patient_review_score_route1_score.json>)、[patient_review_score_route2_score.json](<assets/patient_review_score_route2_score.json>)、[patient_review_score_route3_score.json](<assets/patient_review_score_route3_score.json>)，三条 route 在 `patient` 这一题当前总分都是 `78`，且 `qualified=true`。
- 已直接核对 [patient_review_score_route1_score_summary.md](<assets/patient_review_score_route1_score_summary.md>)、[patient_review_score_route2_score_summary.md](<assets/patient_review_score_route2_score_summary.md>)、[patient_review_score_route3_score_summary.md](<assets/patient_review_score_route3_score_summary.md>)，当前软层总评分别是 `82 / 72 / 72`。
- 已直接核对 [patient_review_score_route1_manifest.json](<assets/patient_review_score_route1_manifest.json>)、[patient_review_score_route2_manifest.json](<assets/patient_review_score_route2_manifest.json>)、[patient_review_score_route3_manifest.json](<assets/patient_review_score_route3_manifest.json>)，三条 route 当前状态都已是 `completed`。
