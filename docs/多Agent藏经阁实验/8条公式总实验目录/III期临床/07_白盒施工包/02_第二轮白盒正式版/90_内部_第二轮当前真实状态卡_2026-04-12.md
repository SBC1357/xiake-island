# 第二轮历史状态卡（2026-04-12）

- 角色：本文件只保留第二轮当时现场的内部状态快照，不再承担当前阶段 / 当前阻塞 / 当前下一步的唯一真相源。
- 当前活状态请回到：[III期临床当前阶段任务卡](../../00_入口索引/03_当前阶段任务卡.md)
- 当前执行与验证真相源请回到：[白盒系统第四轮收口长线程包.md](../04_第四轮白盒测试+建设/白盒系统第四轮收口长线程包.md)
- 历史状态：`partial`，当时未收口。
- 对应 runtime 快照：`$RUNTIME/iiia_module_pk/module_pk_20260412_191938_693`
- 对应阶段：事故接管后的 runtime 证据回收与半途续跑（第二轮历史现场）。

> 注：`$RUNTIME` = 仓库外 runtime 目录（侠客岛-runtime），不在仓库内。
>
> 下文出现的“当前”，均指 `2026-04-12` 第二轮当时现场，不代表现在的 III 期临床活状态。

## 当时已直接核实

1. `baseline_freeze` 当前已经有三题：
   - `lecanemab_patient`
   - `lecanemab_mechanism`
   - `lecanemab_news`

2. 这次续跑日志已经直接证明：
   - 先补出了 `baseline_freeze\lecanemab_mechanism`
   - 再补出了 `baseline_freeze\lecanemab_news`
   - 之后继续进入模块续跑

3. `whole_revise` 当前三条路线下的三题目录都已落出，且 `mechanism/news` 的 `revise_manifest.json` 已直接存在。

4. `local_task_router` 当前已直接核实到：
   - `route1` 的 `mechanism/news` 已落出 `local_revise_manifest.json`
   - `route2` 只推进到 `lecanemab_mechanism` 开跑
   - `route3` 目前仍只有 `patient`

5. `local_patch` 与 `local_patch_audit` 当前仍只看到 `patient`，`mechanism/news` 还没有补齐。

## 当时最关键的口径限制

1. 不能写“第二轮已收口”。
2. 不能写“六模块三路线三题都已有效跑完”。
3. 不能把 `draft` / `review_score` 现在补出来的 `mechanism/news` 目录直接当成 route-specific 有效结果。

原因：

- 续跑日志直接显示，这两类目录当前是先 `seeded`，随后因为目录内已有 `completed` 判定而被 `SKIP`。
- 也就是说，当前 `draft` / `review_score` 的 `mechanism/news` 至少需要二次核验，不能只凭目录存在就算实验完成。

## 当时阻塞

1. 续跑在 `local_task_router/route2/lecanemab_mechanism` 之后被人工中断，后续模块没有跑完。
2. `local_patch` / `local_patch_audit` 还没有把 `mechanism/news` 补齐。
3. 顶层 `module_pk_summary.json` 的时间早于本次续跑日志最后写入时间，不能代表最新现场状态。

## 当时下一步

1. 先修白盒环境隔离：
   - 清全局污染变量
   - 子脚本前再次清理
   - 污染即硬阻断
   - 开跑前打印实际生效的 profile / model / base_url / protocol

2. 再从当前 runtime 继续补跑剩余模块：
   - `local_task_router` 剩余项
   - `local_patch`
   - `local_patch_audit`

3. 最后重建顶层 `module_pk_summary.json`，再决定是否能写第二轮正式结论。

## 证据摘要

- 已直接核对 `baseline_freeze` 目录，三题都存在，且 `mechanism/news` 的 `LastWriteTime` 为 `2026-04-12 22:08:35`。
- 已直接核对 [resume_20260412_220835.log](<../../../../../../../侠客岛-runtime/iiia_module_pk/module_pk_20260412_191938_693/logs/resume_20260412_220835.log>)，其中明确出现：
  - `[FREEZE] created ... lecanemab_mechanism`
  - `[FREEZE] created ... lecanemab_news`
  - `[EXEC] whole_revise/route1/lecanemab_mechanism`
  - `[EXEC] local_task_router/route1/lecanemab_mechanism`
- 已直接核对：
  - [runs/whole_revise/route1/lecanemab_mechanism/revise_manifest.json](<../../../../../../../侠客岛-runtime/iiia_module_pk/module_pk_20260412_191938_693/runs/whole_revise/route1/lecanemab_mechanism/revise_manifest.json>)
  - [runs/whole_revise/route1/lecanemab_news/revise_manifest.json](<../../../../../../../侠客岛-runtime/iiia_module_pk/module_pk_20260412_191938_693/runs/whole_revise/route1/lecanemab_news/revise_manifest.json>)
  - [runs/local_task_router/route1/lecanemab_mechanism/local_revise_manifest.json](<../../../../../../../侠客岛-runtime/iiia_module_pk/module_pk_20260412_191938_693/runs/local_task_router/route1/lecanemab_mechanism/local_revise_manifest.json>)
  - [runs/local_task_router/route1/lecanemab_news/local_revise_manifest.json](<../../../../../../../侠客岛-runtime/iiia_module_pk/module_pk_20260412_191938_693/runs/local_task_router/route1/lecanemab_news/local_revise_manifest.json>)
