# Q4 / 样稿增益组首题 / 全貌

状态：blocked
日期：2026-04-06
组别：IID / 样稿增益组
候选题目代号：ctad_p096_c（备选：sindi_gastric_esmo_c）
说明：

- 本题属于第 2~7 题阶段性放开条件段中的"样稿增益组首题"
- 当前被判定为 **blocked**，不执行写作链
- 本文件记录 blocked 原因、证据与后续解除条件

---

## 一、blocked 原因

### 1. 白盒管线中不存在样稿输入机制

以下搜索均返回零结果：

- `grep_search` 在 `scripts/` 目录下搜索 `样稿|sample_article|exemplar` → **0 匹配**
- `grep_search` 在 `scripts/` 目录下搜索 `reference_sample|参考样稿|style_anchor` → **0 匹配**

当前白盒管线链路为：orchestrator → contract → materials → draft → score。其中：

- `writing_contract` 有 `must_include_facts`、`required_structure`、`hard_rules` 等字段
- `input_contract` 有原始材料透传
- **没有任何字段或通道用于传入"参考样稿"作为风格/结构锚点**

### 2. 实验方案中样稿变量的正式状态

根据 [七十一条总留痕](../8条公式总实验目录/03_过程强留痕/00_总留痕.md) 的正式定义：

- `IID` 分为 A臂（无样稿基线臂）和 B臂（条件样稿臂）
- B臂"只有样稿变量被正式冻结后才允许启动"
- 当前样稿变量**未冻结**：未确认样稿资产、选择人、可见阶段和防泄露边界

### 3. gold/reference 答案不等于样稿

题库中部分题目存在 `gold/` 目录或 `reference_answer.txt`，但这些属于**参考答案**而非**参考样稿**。根据 [状态卡第 11 条硬规则](../8条公式总实验目录/02_阶段实验结果/II期/00_状态卡.md)：

> `参考样稿` 未冻结前，不得把它写成 `IID` 的统一开跑前置条件；第 `1` 题盲测默认不得吃样稿，第 `2~7` 题与最后 `3` 题只有在对应段明确放开后才允许把样稿作为结构/风格锚点使用。

---

## 二、后续解除条件

Q4 从 blocked 转为可执行，需要同时满足：

1. **样稿资产确认**：正式指定哪些文件是参考样稿（区分 gold/answer vs 样稿/style anchor）
2. **选择人冻结**：明确样稿由谁选定（用户 / 系统 / 固定文件）
3. **可见阶段冻结**：明确样稿在写作链的哪个环节可见（出稿前 / 出稿后 / 审核环节）
4. **防泄露边界冻结**：明确样稿可见内容不包含 gold 或参考答案
5. **白盒管线接口实现**：在 contract / materials / draft 中实现样稿输入通道

以上 5 项全部冻结后，Q4 方可选定具体题目并执行。

---

## 三、当前可直接做的事

- 无。Q4 blocked 不是工程故障，而是实验设计层面的变量未冻结。
- 后续如用户正式冻结样稿变量，Q4 可立即从当前候选（ctad_p096_c / sindi_gastric_esmo_c）中选定并执行。

---

## 文末工程附录目录

- runtime 证据链落盘目录：`D:\汇度编辑部1\侠客岛-runtime\iid_q4_sample_article\20260406_210715_919_blocked\`
- blocked 证据：grep_search 结果（零匹配）+ 实验方案 B臂未冻结判断
