# 侠客岛仓库清理审计报告

- 日期: 2026-03-28
- 状态: 审计已完成；2026-03-28 已创建冻结副本，并在主仓执行第一批高置信清理
- 仓库路径: `/mnt/d/汇度编辑部1/侠客岛`
- 扫描路线: `Route C / 深度取证扫`
- 结论口径: 这份报告区分 `可立即删`、`复制后复核再删`、`暂不建议动`。考虑到当前版本已上云，本报告默认建议先复制一份工作副本，再在副本上执行清理。

## 0. 执行状态更新

- 冻结副本已创建到 `D:\侠客岛写作系统副本\侠客岛`，保留了清理前的工作区状态，用于回溯对照。
- 当前主仓已执行第一批高置信清理：`.venv/`、`frontend/node_modules/`、`frontend/dist/`、`.pytest_cache/`、`.opencode/node_modules/`、`xiakedao.egg-info/`、`.git/cursor/`、`beta_server.log`、以及 `src/tests/scripts` 下的 `__pycache__/` 和 `.pyc`。
- 当前主仓未动的内容包括：`data/tasks.db`、`src/data/uploads/`、`docs/checkpoints/`、`checkpoints/`、协作文档与大部分业务文档。
- 主仓已新增根 `.gitignore`，用于拦截本轮清掉的本地生成物再次作为未跟踪垃圾回流。

## 1. 总结结论

本仓库的主要冗余不在业务文档，而在本地环境、构建产物、编译缓存、运行时数据和会话工具残留。

按当前扫描结果：

- 高置信可删项主要集中在 `.venv/`、`frontend/node_modules/`、`frontend/dist/`、`.pytest_cache/`、`.opencode/node_modules/`、源码与测试目录中的 `__pycache__/` 和 `.pyc`、`beta_server.log`、`xiakedao.egg-info/`、`.git/cursor/`。
- 需要复制后再判断的主要是 `src/data/uploads/`、`data/tasks.db`、以及根目录 3 个 OpenCode/Qoder 协作文档。
- `docs/checkpoints/`、`checkpoints/`、大部分 `docs/` 文档当前不建议直接删，因为它们在其他文档中有明确引用链。

## 2. 仓库体积与结构信号

按目录体积看，当前主要占用如下：

| 路径 | 体积 | 说明 |
| --- | ---: | --- |
| `frontend/` | 214M | 主要由 `node_modules/` 和 `dist/` 构成 |
| `.venv/` | 77M | 本地 Python 虚拟环境 |
| `.git/` | 42M | 版本库本体，不作为常规清理对象 |
| `src/` | 9.6M | 含源码和运行时上传缓存 |
| `.opencode/` | 5.9M | 工具本地依赖与计划文件 |
| `data/` | 2.9M | 任务日志数据库 |
| `tests/` | 2.2M | 含测试与测试缓存痕迹 |
| `docs/` | 804K | 文档本体，不是主要空间来源 |

仓库当前共扫描到约 `23827` 个文件、`1940` 个目录、`4` 个符号链接。排除 `.git/objects`、`.venv`、`frontend/node_modules`、`.opencode/node_modules` 后，仓库仍有 `714` 个文件，其中仅 `.pyc` 就有 `268` 个。

## 3. 可立即删

这些项目要么是可重建产物，要么是本地缓存、日志或索引，删除后不会损伤源码与正式文档本体。若你准备复制一份出去做优化，建议优先在副本中删这一批。

### 3.1 本地依赖与构建产物

| 路径 | 体积/数量 | 判断 |
| --- | ---: | --- |
| `frontend/node_modules/` | `13903` 文件，目录约 `214M` 的主体 | 前端依赖目录，`frontend/.gitignore` 已忽略，可由 `package-lock.json` 重建 |
| `.venv/` | `3065` 文件，约 `77M` | Python 本地虚拟环境，`.venv/.gitignore` 自身忽略全部内容，可重建 |
| `frontend/dist/` | `5` 文件，约 `548K` | Vite 构建输出，`frontend/.gitignore` 已忽略；其中 `favicon.svg`、`icons.svg` 与 `frontend/public/` 下同名文件哈希一致 |
| `.pytest_cache/` | `5` 文件，约 `16K` | pytest 缓存目录，`.pytest_cache/.gitignore` 自身忽略全部内容 |
| `.opencode/node_modules/` | `782` 文件，约 `5.9M` 中的大头 | OpenCode 本地依赖目录，`.opencode/.gitignore` 已忽略 |
| `.opencode/node_modules/.old-EB915AAF8DD6D52A/` | 约 `444K` | 典型依赖升级残留旧目录，且与 `@opencode-ai/sdk/dist/` 多文件哈希重复 |
| `xiakedao.egg-info/` | 约 `4K` | Python 打包元数据目录，未见被源码依赖 |

### 3.2 Python 编译缓存

| 路径 | 体积/数量 | 判断 |
| --- | ---: | --- |
| `src/**/__pycache__/` | 共 `29` 个缓存目录的一部分 | 编译缓存，不应作为源码资产保留 |
| `tests/**/__pycache__/` | 同上 | 测试编译缓存 |
| `scripts/__pycache__/` | 未跟踪缓存目录 | 脚本编译缓存 |
| 所有 `.pyc` | 工作区共 `268` 个，约 `3.08M` | 纯生成物 |
| 被 Git 跟踪的 `.pyc` | `258` 个；当前仍存在 `233` 个，约 `2.70M` | 这是仓库最明显的“应清但先别在现仓直接动”的噪声源 |

说明：

- 当前 `git status --short --ignored=matching` 显示，大量 `.pyc` 处于 `M`、`D`、`??` 混杂状态，说明它们既被跟踪，又在本地持续重生成。
- 根目录没有 `.gitignore`，这也是这些 Python 产物长期混进仓库的重要原因之一。

### 3.3 日志与编辑器索引

| 路径 | 体积 | 判断 |
| --- | ---: | --- |
| `beta_server.log` | `6323` 字节 | 本地服务启动失败日志，可删 |
| `.git/cursor/` | 约 `2.1M` | Cursor/IDE 在 `.git` 下写入的本地索引，不是版本库核心对象，可删后再生 |

## 4. 复制后复核再删

这些项目不是源码本体，但可能承载运行历史、上传样本或一次性协作记录。更适合先复制副本，再决定是否保留、归档或清空。

### 4.1 运行时上传缓存

| 路径 | 体积/数量 | 风险 |
| --- | ---: | --- |
| `src/data/uploads/` | `3` 个上传目录，共约 `7.07M` | 删除会丢失本地上传样本与转换结果 |

具体证据：

- `src/modules/evidence/services/upload_service.py` 与 `src/modules/evidence/services/image_converter.py` 都把上传文件默认落在 `src/data/uploads/`。
- `upload_19f5b0720d07/` 与 `upload_cbd04eaa072f/` 两组文件完全重复：
  - `original.pdf` 哈希一致
  - `images/page_1.png` 哈希一致
- `upload_feb6cdb9fc3f/` 中 `original.png` 与 `images/page_1.png` 哈希一致，说明这是一次单页图片转换缓存。

建议：

- 如果复制出去的版本只需要代码和文档，不需要保留已上传样本，可整批清空 `src/data/uploads/`。
- 如果还要保留演示或回放材料，可至少先去掉重复的 `upload_19f5b0720d07/` 与 `upload_cbd04eaa072f/` 之一。

### 4.2 任务日志数据库

| 路径 | 体积 | 风险 |
| --- | ---: | --- |
| `data/tasks.db` | `2.87M` | 删除会丢失本地任务与审计历史 |

具体证据：

- `src/runtime_logging/sqlite_store.py` 默认把任务日志写入 `./data/tasks.db`。
- 现库内有：
  - `tasks` 表记录 `289` 条
  - `audit_events` 表记录 `111` 条
  - 最近任务开始时间为 `2026-03-25T16:14:13.249294+00:00`

建议：

- 如果复制出去是为了“干净优化版”，可不带此数据库，或先导出再删。
- 如果你后续还要对照历史任务轨迹排查问题，建议改为归档保存，而不是直接删除。

### 4.3 会话与协作文档

| 路径 | 判断 | 风险 |
| --- | --- | --- |
| `OPENCODE_SESSION_ANCHOR.md` | 旧会话锚点 | 删除会失去旧会话定位信息 |
| `SHARED_MAILBOX.md` | 旧协作信箱 | 删除会失去一段人工/代理沟通记录 |
| `TO_PLANNER_WINDOW.md` | 临时沟通文件 | 删除会失去一次问题说明与临时测试文本 |

具体证据：

- 这 3 个文件只在彼此之间互相引用，未被源码或正式文档使用。
- 内容均指向 `2026-03-17` 的 OpenCode/Qoder 协作过程，属于明显的阶段性工作物，不像长期产品文档。

建议：

- 如果你要的是“干净可持续的优化副本”，这 3 个文件适合移出主仓，放到单独 `archive/` 或外部留档。
- 如果你还要回看那次协作上下文，可先归档再删。

## 5. 暂不建议动

### 5.1 `docs/checkpoints/` 与 `checkpoints/`

虽然这些文件看起来像阶段性产物，但当前不建议直接删。

原因：

- `docs/archive/root-historical/长线程续接交接_20260318.md`、`docs/archive/root-historical/侠客岛_编辑部工作台与质量闭环执行单_20260316.md`、`docs/archive/root-historical/侠客岛长线程阶段拆分与审核要点_20260318.md` 等多份文档都在直接引用 `docs/checkpoints/` 下的具体文件。
- `checkpoints/` 目录虽然是未跟踪的，但也被 `docs/checkpoints/编辑部工作台_batch-A-2_checkpoint.md` 这类文档引用。

结论：

- 这批更适合后续做“归档策略”而不是本轮删除。

### 5.2 `docs/` 主体文档与 `视觉草案/架构.html`

当前没有足够证据证明它们是无效冗余。

- `视觉草案/架构.html` 是未跟踪草案文件，但只有 `16K`，且名字显示它仍可能对后续前端/架构整理有参考价值。
- `docs/` 主体文档体积仅约 `804K`，不是仓库膨胀主因。

## 6. 分批建议

### 第一批：复制副本后优先清理

- `frontend/node_modules/`
- `.venv/`
- `frontend/dist/`
- `.pytest_cache/`
- `.opencode/node_modules/`
- `.opencode/node_modules/.old-EB915AAF8DD6D52A/`
- `xiakedao.egg-info/`
- `beta_server.log`
- `.git/cursor/`
- 所有 `__pycache__/`
- 所有 `.pyc`

这批的特点是：基本都可重建，不依赖业务语义。

### 第二批：确认是否需要保留运行历史后再动

- `src/data/uploads/`
- `data/tasks.db`
- `OPENCODE_SESSION_ANCHOR.md`
- `SHARED_MAILBOX.md`
- `TO_PLANNER_WINDOW.md`

这批的特点是：删掉不会伤源码，但会伤历史痕迹、演示材料或沟通留痕。

### 第三批：先不删，后续做归档策略

- `docs/checkpoints/`
- `checkpoints/`
- `docs/` 中各种执行单、汇报、方案、阶段记录
- `视觉草案/架构.html`

这批的特点是：更像“项目记忆”，不是主要空间负担。

## 7. 建议的复制与优化顺序

1. 先完整复制当前仓库，保留一份原样副本。
2. 在副本中先执行第一批清理。
3. 运行一次基础启动/测试，确认副本仍可用。
4. 再决定第二批中的运行数据和协作文档是否保留。
5. 最后再评估是否需要把 `docs/checkpoints/` 一类材料迁出主仓，改放到专门归档目录。

## 8. 证据摘要

- 目录体积核对：`frontend/` 约 `214M`、`.venv/` 约 `77M`、`.git/` 约 `42M`、`.opencode/` 约 `5.9M`，仓库主要膨胀来自本地依赖和缓存，而非业务文档。
- Git 状态核对：`git status --short --ignored=matching` 显示大量 `__pycache__/`、`.pyc`、`.venv`、`.pytest_cache`、`frontend/dist/`、`frontend/node_modules/` 混入工作区；其中被 Git 跟踪的 `.pyc` 达 `258` 个。
- 内容哈希核对：`src/data/uploads/upload_19f5b0720d07/` 与 `src/data/uploads/upload_cbd04eaa072f/` 的 `original.pdf` 和 `images/page_1.png` 完全重复；`frontend/dist/` 内静态资源与 `frontend/public/` 同名资源哈希一致；`.opencode/node_modules/.old-EB915AAF8DD6D52A/` 与正式 SDK 目录存在多文件哈希重复。
