# 侠客岛分批清理验证日志

- 日期: 2026-03-28，更新至 2026-03-29
- 状态: 首轮执行已收口；`Batch 0 / 归位批`、`Batch 3 / 运行历史批`、`Batch 4 / 协作文档批` 已完成；`Batch 2 / 构建辅助物批` 暂缓；`checkpoints` 评估已完成；`checkpoints` 第一批与第二批归档迁出已执行；`docs` 根目录第二轮与第三轮归档已执行；`Batch 5 / 历史开发文档删除批` 已执行；`Batch 6 / 私有仓切换批` 已执行；优化方案已形成；`P0 / 运行态出仓` 已实现并验证；`P1 / 主链测试 hermetic 化` 已实现并验证；`P1 / 上传链持久化` 已实现基础版并验证；`P2 / 入口解耦` 已实现基础版并验证；`P3 / 路径与命名治理` 已实现基础版并验证
- 主仓路径: `/mnt/d/汇度编辑部1/侠客岛`
- 冻结副本: `D:\侠客岛写作系统副本\侠客岛`
- 协作仓基线提交: `6650fda` (`master`)
- 私有仓首提: `f038b29` (`main`)
- 目标: 按批次删除可疑或可重建内容；每一批删除后验证运行逻辑是否变化；记录净缩减与测试副产物；保留完整回溯线索

## 1. 当前判定口径

本日志记录的是 `删一批 -> 验一批 -> 清本轮副产物 -> 再进入下一批` 的执行过程。

本轮删除通过以下门槛判断是否“未伤运行主链”：

1. 后端入口验证:
   - 用 `.venv/Scripts/python.exe` 进程内加载应用
   - `XIAGEDAO_STRICT_MODE=false`
   - `/health` 返回 `200`
   - `/` 返回 `200`
2. 前端入口验证:
   - `frontend` 下 `npm run build` 成功
   - `frontend` 下 `npm run test:run` 成功
3. 主链理解边界:
   - `src/main.py` 负责统一 Web 入口，并在 `frontend/dist` 存在时挂载前端静态页
   - `src/api/app.py` 在应用生命周期内校验 `XIAGEDAO_CONSUMER_ROOT`
   - `src/orchestrator/service.py` 的标准链为 `Evidence -> Planning -> Writing -> Drafting -> Quality -> Delivery`
   - 上传链默认写入外部 `runtime_root/uploads/`
   - 任务日志默认写入外部 `runtime_root/data/tasks.db`

## 2. 已核实的基线

### 2.1 基础运行验证

- 后端入口基线: 已验证
  - `TestClient` 访问 `/health` 返回 `200`
  - `TestClient` 访问 `/` 返回 `200`
  - `/` 的 `content-type` 为 `text/html; charset=utf-8`
- 前端入口基线: 已验证
  - `npm run build` 成功
  - `npm run test:run` 通过 `11` 个测试文件、`71` 个测试

### 2.2 已知基线红项

- 历史红项已收敛，不再作为当前基线阻塞项：
  - `tests/test_orchestrator.py` 相关 app-level 红项已通过 fake provider 固化回归门槛解决
  - `register` 相关 Pydantic 告警已通过 alias 兼容改法收敛
- 当前口径：
  - 删除回归验证可直接使用现有后端/前端测试
  - 真实模型 smoke test 仍然与日常回归分层维护

## 3. 当前现场基线

### 3.1 运行必需项

| 路径 | 当前状态 | 已核实信息 | 本轮策略 |
| --- | --- | --- | --- |
| `.venv/` | 存在 | 约 `135M` | 保留，作为后端验证环境 |
| `frontend/node_modules/` | 存在 | 约 `210M` | 保留，作为前端验证环境 |
| `frontend/dist/` | 存在 | `5` 文件，约 `548K` | 先保留，后续单独测 |

### 3.2 本轮先处理或观察的对象

| 路径 | 当前状态 | 已核实信息 | 当前结论 |
| --- | --- | --- | --- |
| `.pytest_cache/` | 存在 | `5` 文件，约 `12K` | 这是验证副产物，可删后再验 |
| `.git/cursor/` | 不存在 | 当前已空 | 本轮只记状态 |
| `beta_server.log` | 不存在 | 当前已空 | 本轮只记状态 |
| `xiakedao.egg-info/` | 存在 | `5` 文件，约 `12K` | 归位批候选 |
| `.opencode/node_modules/` | 存在 | `739` 文件，约 `5.4M` | 归位批候选 |
| `data/tasks.db` | 存在 | 约 `2.9M` | 延后，属运行历史 |
| `src/data/uploads/` | 存在 | 约 `7.1M` | 延后，属上传样本与转换结果 |

### 3.3 当前副产物污染

- 基线扫描时已确认:
  - `src/` 与 `tests/` 下共有 `25` 个 `__pycache__` 目录
  - `src/` 与 `tests/` 下共有 `109` 个 `.pyc/.pyo` 文件
- 这些内容部分来自本轮验证过程，本日志后续一律在每批验证完成后清理，并把“清理后状态”单独记账。

## 4. 批次设计

### Batch 0 / 归位批

- 目标:
  - 撤回前一轮为“跑通路径”而额外恢复、但并非前后端主链必需的内容
- 候选:
  - `.opencode/node_modules/`
  - `xiakedao.egg-info/`
- 删除后验证:
  - 后端入口验证
  - 前端入口验证
  - 清理本轮产生的 `.pytest_cache`、`__pycache__`、`.pyc`

### Batch 1 / 低风险副产物批

- 候选:
  - `.pytest_cache/`
  - 所有 `__pycache__/`
  - 所有 `.pyc/.pyo`
  - `beta_server.log`
  - `.git/cursor/`
- 删除后验证:
  - 后端入口验证
  - 前端入口验证

### Batch 2 / 构建辅助物批

- 候选:
  - `frontend/dist/`
- 删除后验证:
  - 重新 `npm run build`
  - 后端入口验证，确认静态页重新生成后仍可挂载

### Batch 3 / 运行历史批

- 候选:
  - `data/tasks.db`
  - `src/data/uploads/`
- 删除前附加条件:
  - 需要先完成导出或确认不保留历史样本

### Batch 4 / 协作文档批

- 候选:
  - `OPENCODE_SESSION_ANCHOR.md`
  - `SHARED_MAILBOX.md`
  - `TO_PLANNER_WINDOW.md`
  - 之后再评估 `checkpoints/` 与 `docs/checkpoints/`

## 5. 行为记录

### 5.1 基线建立

1. 确认冻结副本已存在，主仓为执行对象。
2. 复核统一 Web 入口、应用生命周期、标准六段链、上传目录与任务库位置。
3. 复测前端 `build` 与前端测试，结果通过。
4. 复测后端 `/health` 与 `/`，结果通过。
5. 复跑后端 pytest 子集，确认存在既有失败项，不把它当删除回归信号。
6. 统计本轮目标目录和副产物现状，作为 Batch 0 起点。

### 5.2 待执行

- 下一步进入 `Batch 0 / 归位批`
- 执行后将在本日志追加:
  - 删除项
  - 删除前后文件数或目录数
  - 删除前后体积变化
  - 验证结果
  - 本轮副产物清理结果

### 5.3 Batch 0 / 归位批

- 执行动作:
  - 删除 `.opencode/node_modules/`
  - 删除 `xiakedao.egg-info/`
- 删除前基线:
  - `.opencode/node_modules/`: `739` 文件，约 `5.4M`
  - `xiakedao.egg-info/`: `5` 文件，约 `12K`
- 删除后状态:
  - 两个路径均已不存在
- 删除后验证:
  - 后端入口验证通过
    - `/health = 200`
    - `/ = 200`
  - 前端入口验证通过
    - `npm run build` 成功
    - `npm run test:run` 通过 `11` 个测试文件、`71` 个测试
- 本轮验证副产物清理:
  - 清理前:
    - `src/tests/scripts` 下共有 `25` 个 `__pycache__` 目录
    - `src/tests/scripts` 下共有 `82` 个 `.pyc/.pyo` 文件
    - 根 `.pytest_cache/` 中 `0` 个文件，但目录存在
  - 清理后:
    - `__pycache__` 目录归零
    - `.pyc/.pyo` 文件归零
    - `.pytest_cache/` 不存在
- 本批结论:
  - 这两项不在侠客岛写作系统当前主运行链上
  - 删除后前后端入口与前端测试均未出现新失败

### 5.4 Batch 1 / 低风险副产物批

- 这一批没有单独起一轮前后端复测，而是并入 Batch 0 和 Batch 3 的验证收尾。
- 原因:
  - 这批对象本身就是验证过程会再生的缓存
  - 如果为了验证它们再单独跑一次完整测试，会再次生成同类缓存，造成“为验证而回流”
- 当前执行结果:
  - `.pytest_cache/` 已删除
  - `src/tests/scripts` 下的 `__pycache__` 已删除
  - `src/tests/scripts` 下的 `.pyc/.pyo` 已删除
  - `.git/cursor/` 与 `beta_server.log` 当前均不存在

### 5.5 Batch 2 / 构建辅助物批

- 当前暂缓执行 `frontend/dist/` 的最终删除。
- 原因:
  - 当前主仓的后端 `/` 路由依赖 `frontend/dist/`
  - 本轮目标不是只证明“可重建”，而是同时保持主仓可直接验证路径
- 当前结论:
  - `frontend/dist/` 仍然是可重建物
  - 但在“主仓保持可直接跑通”的前提下，本轮不把它作为最终删除项

### 5.6 Batch 3 / 运行历史批

- 执行动作:
  - 删除 `data/tasks.db`
  - 删除 `src/data/uploads/`
- 删除前基线:
  - `data/tasks.db`: 约 `2.9M`
    - `tasks` 表 `289` 条
    - `audit_events` 表 `111` 条
  - `src/data/uploads/`: `3` 个上传目录、`6` 个文件、约 `7.1M`
- 删除后验证:
  - 前端入口验证通过
    - `npm run build` 成功
    - `npm run test:run` 通过 `11` 个测试文件、`71` 个测试
  - 后端入口验证通过
    - `/health = 200`
    - `/ = 200`
  - 任务库自恢复验证通过
    - `SQLiteTaskLogStore` 在缺失情况下重新创建 `data/tasks.db`
    - 新库内存在 `tasks`、`audit_events` 两张表
  - 上传目录自恢复验证通过
    - `UploadService` 在缺失情况下重新创建 `src/data/uploads/`
    - 成功写入一次探针文件 `cleanup_probe.png`
- 本轮验证副产物与探针清理:
  - 上传探针目录已删除
  - 空 `src/data/uploads/` 根目录已删除
  - 重建出的空 `data/tasks.db` 在 Python 进程内首次删除时遇到文件锁，随后通过 shell 删除成功
  - 清理前:
    - `src/tests/scripts` 下共有 `22` 个 `__pycache__` 目录
    - `src/tests/scripts` 下共有 `103` 个 `.pyc/.pyo` 文件
  - 清理后:
    - `__pycache__` 目录归零
    - `.pyc/.pyo` 文件归零
    - `.pytest_cache/` 不存在
- 本批结论:
  - 删除运行历史后，主入口与前端验证仍然正常
  - `data/tasks.db` 与 `src/data/uploads/` 可以按需自动重建
  - 当前主仓保留“已验证可自恢复、但默认保持缺失”的干净状态

### 5.7 Batch 4 / 协作文档批

- 删前引用确认:
  - `rg -n "OPENCODE_SESSION_ANCHOR|SHARED_MAILBOX|TO_PLANNER_WINDOW" .`
  - 命中位置仅在这 3 个文件自身、审计报告、分批日志中
  - 未命中源码运行链
- 删除前基线:
  - `OPENCODE_SESSION_ANCHOR.md`: `517` 字节
  - `SHARED_MAILBOX.md`: `3046` 字节
  - `TO_PLANNER_WINDOW.md`: `1610` 字节
- 执行动作:
  - 删除 `OPENCODE_SESSION_ANCHOR.md`
  - 删除 `SHARED_MAILBOX.md`
  - 删除 `TO_PLANNER_WINDOW.md`
- 删除后验证:
  - 前端入口验证通过
    - `npm run build` 成功
    - `npm run test:run` 通过 `11` 个测试文件、`71` 个测试
  - 后端入口验证通过
    - `/health = 200`
    - `/ = 200`
- 本轮验证副产物清理:
  - 后端检查会再次生成空 `data/tasks.db`，已在本轮收尾再次删除
  - 清理前:
    - `src/tests/scripts` 下共有 `22` 个 `__pycache__` 目录
    - `src/tests/scripts` 下共有 `103` 个 `.pyc/.pyo` 文件
  - 清理后:
    - `__pycache__` 目录归零
    - `.pyc/.pyo` 文件归零
    - `.pytest_cache/` 不存在
- 本批结论:
  - 这 3 份文件属于阶段性协作留痕，不在运行链上
  - 删除后前后端哨兵验证没有出现新失败

### 5.8 当前收口状态

- 当前已保持删除状态的项目:
  - `.opencode/node_modules/`
  - `xiakedao.egg-info/`
  - `data/tasks.db`
  - `src/data/uploads/`
  - `OPENCODE_SESSION_ANCHOR.md`
  - `SHARED_MAILBOX.md`
  - `TO_PLANNER_WINDOW.md`
  - `.pytest_cache/`
  - `src/tests/scripts` 下所有 `__pycache__/`
  - `src/tests/scripts` 下所有 `.pyc/.pyo`
- 当前保留未删的项目:
  - `.venv/`
  - `frontend/node_modules/`
  - `frontend/dist/`
  - `checkpoints/`
  - `docs/checkpoints/`
- 保留原因:
  - `.venv/` 与 `frontend/node_modules/` 是当前主仓直接验证路径所需
  - `frontend/dist/` 仍被后端 `/` 路由使用
  - `checkpoints/` 与 `docs/checkpoints/` 属项目记忆材料，已有文档引用链，未纳入本轮正式删除

### 5.9 `checkpoints/` 与 `docs/checkpoints/` 评估

- 本轮动作:
  - 只做结构、引用链和文档族群评估
  - 不做删除或迁移

#### 5.9.1 目录现状

- `checkpoints/`
  - 体积约 `216K`
  - 当前共 `3` 个文件
  - Git 状态为未跟踪
- `docs/checkpoints/`
  - 体积约 `332K`
  - 当前共 `62` 个文件
  - 其中 `61` 个为 markdown，`1` 个为 Python 脚本
  - Git 状态为未跟踪

#### 5.9.2 引用链结论

- `docs/checkpoints/` 不能整目录直接删：
  - 在限定到文档语料的引用统计中，`61` 个 markdown 中有 `40` 个存在入链引用
  - 强引用主要来自：
    - `docs/archive/root-historical/长线程续接交接_20260318.md`
    - `docs/archive/root-historical/侠客岛_编辑部工作台与质量闭环执行单_20260316.md`
    - `docs/archive/root-historical/侠客岛长线程阶段拆分与审核要点_20260318.md`
    - `docs/archive/root-historical/独立成稿模块改造长线程执行单_20260319.md`
- `docs/checkpoints/` 内有 `21` 个 markdown 当前无外部入链，合计仅约 `60990` 字节，约 `60K`
  - 其中 `17` 个属于 `三系统解耦*`
  - `1` 个属于 `编辑部工作台*`
  - 其余 `3` 个为单独文件
- 根 `checkpoints/` 不是整目录同质：
  - `checkpoints/2026-03-17-empty-dict-serialization-fix.md` 仍被 `docs/checkpoints/编辑部工作台_batch-A-2_checkpoint.md` 引用
  - `checkpoints/2026-03-24-frontend-backend-unified-modification.md` 当前无任何外部引用
  - `checkpoints/image/2026-03-24-frontend-backend-unified-modification/1774316984971.png` 当前无任何文本引用，约 `208198` 字节，约 `203K`

#### 5.9.3 特殊文件判断

- `docs/checkpoints/phase6_dryrun_test.py`
  - 不是运行代码的一部分
  - 但被 `docs/archive/root-historical/三系统解耦基线施工汇报_20260315.md` 明确点名为执行脚本
  - 更适合跟随对应汇报归档，而不是孤立删除
- `docs/checkpoints/编辑部工作台_batch-A-2_checkpoint_20260316.md`
  - 当前无入链
  - 与 `docs/checkpoints/编辑部工作台_batch-A-2_checkpoint.md` 同题材
  - 后者包含后续 `2026-03-17` 的修复补记并实际引用了根 `checkpoints/2026-03-17-empty-dict-serialization-fix.md`
  - 可视为“较早版本”，倾向归档而非继续留在主路径
- `docs/checkpoints/最小监督链验证_checkpoint_20260319.md`
  - 当前仅 `106` 字节
  - 内容更像一次会话通过标记
  - 当前无入链，是强归档候选

#### 5.9.4 处理建议

- `docs/checkpoints/`:
  - 不建议整目录直接删除
  - 更合适的动作是“分层归档”
  - 第一层保留：有入链引用的 `40` 个 markdown
  - 第二层归档候选：当前无入链的 `21` 个 markdown
- `checkpoints/`:
  - 不建议整目录直接删除
  - 建议拆开处理
  - 保留或随引用一起迁移：`2026-03-17-empty-dict-serialization-fix.md`
  - 强归档候选：`2026-03-24-frontend-backend-unified-modification.md`
  - 强归档候选：`checkpoints/image/2026-03-24-frontend-backend-unified-modification/1774316984971.png`
- 结论口径:
  - 这两处目录都不是“主要空间负担”
  - 它们的问题主要是“项目记忆混在主路径里”，不是“占空间太大”
  - 因此后续更应该做“迁出主路径的归档整理”，而不是追求直接删空

#### 5.9.5 评估输出

- 已新增独立清单：
  - `侠客岛-checkpoints归档迁出清单.md`
- 该清单给出了：
  - `优先迁出`
  - `次级迁出`
  - `原地保留`
  - 建议迁出目标路径

### 5.10 `checkpoints` 第一批归档迁出执行

- 执行动作:
  - 新建归档目录：
    - `docs/archive/checkpoints-root/`
    - `docs/archive/checkpoints-root/image/`
    - `docs/archive/checkpoints-unlinked/`
  - 迁出：
    - `checkpoints/2026-03-24-frontend-backend-unified-modification.md`
    - `checkpoints/image/2026-03-24-frontend-backend-unified-modification/1774316984971.png`
    - `docs/checkpoints/最小监督链验证_checkpoint_20260319.md`
    - `docs/checkpoints/编辑部工作台_batch-A-2_checkpoint_20260316.md`
- 迁出后落点:
  - `docs/archive/checkpoints-root/2026-03-24-frontend-backend-unified-modification.md`
  - `docs/archive/checkpoints-root/image/2026-03-24-frontend-backend-unified-modification/1774316984971.png`
  - `docs/archive/checkpoints-unlinked/最小监督链验证_checkpoint_20260319.md`
  - `docs/archive/checkpoints-unlinked/编辑部工作台_batch-A-2_checkpoint_20260316.md`
- 迁出前依据:
  - `2026-03-24-frontend-backend-unified-modification.md` 历史外部引用数为 `0`
  - `最小监督链验证_checkpoint_20260319.md` 历史外部引用数为 `0`
  - `编辑部工作台_batch-A-2_checkpoint_20260316.md` 历史外部引用数为 `0`
- 迁出后核对:
  - 4 个目标路径均存在
  - 4 个源路径均不存在
  - 图片附件目录已从根 `checkpoints/image/` 下移走
- 迁出后哨兵验证:
  - 前端 `npm run build` 成功
  - 前端 `npm run test:run` 通过 `11` 个测试文件、`71` 个测试
  - 后端 `/health = 200`
  - 后端 `/ = 200`
- 本轮副产物清理:
  - 后端检查再次生成空 `data/tasks.db`，已删除
  - 清理前:
    - `src/tests/scripts` 下共有 `22` 个 `__pycache__` 目录
    - `src/tests/scripts` 下共有 `103` 个 `.pyc/.pyo` 文件
  - 清理后:
    - `__pycache__` 目录归零
    - `.pyc/.pyo` 文件归零
    - `.pytest_cache/` 不存在
- 本批结论:
  - 第一批归档迁出未影响侠客岛写作系统当前主运行链
  - 这批内容已离开主路径，但仍在仓库内可回溯

### 5.11 `checkpoints` 第二批归档迁出执行

- 执行动作:
  - 将 `docs/checkpoints/` 下当前 `19` 个零历史入链 markdown 统一迁入 `docs/archive/checkpoints-unlinked/`
- 迁出前依据:
  - 在排除本轮审计/日志文档后，第二批候选历史入链数均为 `0`
  - 第二批合计约 `58856` 字节，约 `57.5K`
- 迁出后状态:
  - `docs/archive/checkpoints-unlinked/` 当前共 `21` 个文件
  - `docs/checkpoints/` 当前保留 `40` 个 markdown
- 迁出后哨兵验证:
  - 前端 `npm run build` 成功
  - 前端 `npm run test:run` 通过 `11` 个测试文件、`71` 个测试
  - 后端 `/health = 200`
  - 后端 `/ = 200`
- 本轮副产物清理:
  - 后端检查再次生成空 `data/tasks.db`，已删除
  - 清理前:
    - `src/tests/scripts` 下共有 `22` 个 `__pycache__` 目录
    - `src/tests/scripts` 下共有 `103` 个 `.pyc/.pyo` 文件
  - 清理后:
    - `__pycache__` 目录归零
    - `.pyc/.pyo` 文件归零
    - `.pytest_cache/` 不存在
- 本批结论:
  - 第二批归档迁出未影响侠客岛写作系统当前主运行链
  - `docs/checkpoints/` 主路径已收敛为“仍有历史入链的保留集”

### 5.12 优化方案输出

- 已新增独立方案：
  - `侠客岛-系统优化方案_20260329.md`
- 当前方案结论:
  - 优先级最高的是：
    - 运行态出仓
    - 上传链持久化补齐
    - 主链测试 hermetic 化
  - 其后再做：
    - 入口解耦
    - 平台路径与命名噪音治理

### 5.13 `P0 / 运行态出仓` 实现与验证

- 实现动作：
  - 新增统一运行态路径层：
    - `src/runtime_paths.py`
  - 调整默认任务库落点：
    - `src/runtime_logging/sqlite_store.py`
  - 调整默认上传链落点：
    - `src/modules/evidence/services/upload_service.py`
    - `src/modules/evidence/services/image_converter.py`
  - 调整共享日志初始化时机：
    - `src/api/app.py`
  - 调整后端开发脚本：
    - `backend-dev.ps1`
    - `backend-dev.bat`
  - 补充遗留仓库内 runtime 落点忽略：
    - `.gitignore`
- 当前默认行为：
  - 默认 runtime root 为仓库同级目录 `侠客岛-runtime/`
  - 默认任务库落点为 `runtime_root/data/tasks.db`
  - 默认上传链落点为 `runtime_root/uploads/`
  - 仅访问 `/health` 不会创建任务库
  - 首次访问依赖共享 `TaskLogger` 的任务接口时，才会创建外部任务库
- 定向验证：
  - 后端定向 pytest：
    - `tests/test_runtime_paths.py`
    - `tests/runtime_logging/test_sqlite_store.py`
    - `tests/test_main.py`
    - `tests/integration/test_health.py`
    - 结果：`31 passed, 8 warnings`
  - 默认路径探针：
    - `repo_db_exists_after_health = False`
    - `runtime_db_exists_after_health = False`
    - `repo_db_exists_after_tasks = False`
    - `runtime_db_exists_after_tasks = True`
    - `runtime_db_path = D:\汇度编辑部1\侠客岛-runtime\data\tasks.db`
  - 前端哨兵：
    - `frontend npm run build` 成功
    - `frontend npm run test:run` 通过 `11` 个测试文件、`71` 个测试
- 本轮验证副产物清理：
  - 已删除本轮生成的 `.pytest_cache/`
  - 已删除 `src/`、`tests/` 下本轮再生的 `__pycache__/`、`.pyc/.pyo`
  - 已删除默认路径探针生成的外部 `侠客岛-runtime/`
  - 仓库内 `data/tasks.db` 仍保持缺失
- 本批结论：
  - 运行态默认已迁出仓库主路径
  - `P0` 的目标已经达到，并且未破坏当前前后端验证链
  - 下一步应进入 `P1 / 上传链持久化` 与 `P1 / 主链测试 hermetic 化`

### 5.14 `P1 / 主链测试 hermetic 化` 实现与验证

- 实现动作：
  - 新增测试层夹具：
    - `tests/conftest.py`
- 夹具策略：
  - 对 `tests/test_orchestrator.py`
  - 对 `tests/api/`
  - 对 `tests/integration/`
  - 自动强制：
    - `LLM_PROVIDER=fake`
    - `LLM_MODEL=fake-model`
    - `DRAFTING_MODE=fake`
- 实现目的：
  - 避免 app-level 测试被 `.env` 或外部 shell 环境里的 provider 配置污染
  - 保持 provider 自身的单元测试不受影响
- 定向验证：
  - 反证式污染验证：
    - 显式设置 `LLM_PROVIDER=openai`
    - 显式设置 `LLM_MODEL=gpt-4`
  - 在上述污染环境下运行：
    - `tests/test_orchestrator.py`
    - `tests/integration/test_workflow_api.py`
    - `tests/integration/test_opinion_api.py`
    - `tests/integration/test_semantic_review_api.py`
    - `tests/api/test_tasks.py`
  - 结果：`102 passed, 8 warnings`
  - provider 工厂单测抽查：
    - `tests/test_provider_factory.py::TestCreateLLMProviderFromEnv::test_from_env_default_to_fake`
    - 结果：`1 passed`
- 本轮验证副产物清理：
  - 已删除本轮再生的 `__pycache__/`、`.pyc/.pyo`
  - `.pytest_cache/` 保持缺失
- 本批结论：
  - 编排器与 workflow 族的 app-level 回归门槛已不再依赖外部 provider 环境
  - 真实模型 smoke test 仍需后续单列，不应重新混入日常回归

### 5.15 `P1 / 上传链持久化` 基础版实现与验证

- 实现动作：
  - 新增 upload manifest 持久化层：
    - `src/modules/evidence/services/upload_manifest_store.py`
  - 调整上传服务：
    - `src/modules/evidence/services/upload_service.py`
- 当前实现策略：
  - 每个 `upload_id` 目录下新增 `manifest.json`
  - 在这些阶段落盘：
    - 原始文件已接收
    - 图像转换进行中 / 已完成 / 失败
    - 视觉识别进行中 / 已完成 / 失败
    - 可回溯证据已构建完成
  - `UploadService` 初始化时自动扫描现有 manifest 并恢复：
    - `_uploads`
    - `_page_images`
    - `_fragments`
    - `_evidences`
- 定向验证：
  - `tests/modules/test_upload_persistence.py`
  - 结果：`2 passed`
  - 已覆盖场景：
    - 完整上传链完成后，服务重建可恢复上传状态、图像、片段和证据
    - 上传链失败后，服务重建可恢复失败状态和错误信息
- 当前边界：
  - 已补齐“文件和索引同边界持久化”
  - 仍未补齐：
    - 全局索引治理
    - manifest 版本迁移
    - 生命周期清理策略
- 本轮验证副产物清理：
  - 已删除本轮再生的 `__pycache__/`、`.pyc/.pyo`
  - `.pytest_cache/` 保持缺失
- 本批结论：
  - 上传链不再是“文件留下来、服务重启就失忆”
  - `P1 / 上传链持久化` 的基础版目标已达到

### 5.16 `P2 / 入口解耦` 基础版实现与验证

- 实现动作：
  - 调整统一入口：
    - `src/main.py`
  - 调整入口测试：
    - `tests/test_main.py`
- 当前实现策略：
  - 新增 `XIAGEDAO_WEB_MODE`
  - 支持两种模式：
    - `web-bundled`
    - `api-only`
  - 默认仍为 `web-bundled`
  - `api-only` 下跳过 `frontend/dist` 挂载
  - `setup_static_serving()` 已支持对临时 `FastAPI` 实例单独配置，避免测试互相污染
- 定向验证：
  - `tests/test_main.py`
  - bundled 模式：首页返回 `200`
  - bundled 模式：`/v1/not-found` 返回 `404`
  - `api-only` 模式：根路径 `/` 返回 `404`
  - 联合回归：
    - `tests/test_main.py`
    - `tests/modules/test_upload_persistence.py`
    - `tests/test_runtime_paths.py`
    - `tests/runtime_logging/test_sqlite_store.py`
    - `tests/integration/test_health.py`
  - 结果：`34 passed, 8 warnings`
  - 污染环境回归：
    - `LLM_PROVIDER=openai`
    - `LLM_MODEL=gpt-4`
    - 编排器 / workflow / opinion / semantic_review / tasks 相关测试仍通过 `102/102`
- 当前边界：
  - 默认 bundled 入口仍保留，不影响现有启动方式
  - 文档和启动脚本还没有完全展开两种模式的使用说明
- 本轮验证副产物清理：
  - 已删除本轮再生的 `__pycache__/`、`.pyc/.pyo`
  - `.pytest_cache/` 保持缺失
- 本批结论：
  - 后端已经具备显式 `api-only` 运行模式
  - `P2 / 入口解耦` 的基础版目标已达到

### 5.17 `P3 / 路径与命名治理` 基础版实现与验证

- 实现动作：
  - 新增环境适配层：
    - `src/env_utils.py`
  - 调整路径/入口配置：
    - `src/runtime_paths.py`
    - `src/assembly.py`
    - `src/api/app.py`
    - `src/main.py`
  - 调整人工启动脚本：
    - `backend-dev.bat`
    - `backend-dev.ps1`
    - `dev.bat`
    - `dev.ps1`
    - `frontend-dev.bat`
    - `frontend-dev.ps1`
  - 调整 `register` 兼容字段：
    - `src/modules/semantic_review/models.py`
    - `src/modules/planning/models.py`
    - `src/api/routes/semantic_review.py`
    - `src/api/routes/workflow.py`
    - `src/api/routes/independent_review.py`
  - 调整上传链运行态清理：
    - `src/modules/evidence/services/upload_service.py`
    - `src/api/routes/upload.py`
- 当前实现策略：
  - 统一 `.env` 加载、布尔开关、CSV 环境变量和 WSL/Windows 路径转换
  - 新增 `XIAGEDAO_ENV_FILE`、`XIAGEDAO_FRONTEND_DIST`、`XIAGEDAO_FRONTEND_HOST`、`XIAGEDAO_FRONTEND_PORT`、`XIAGEDAO_CORS_ORIGINS`
  - `XIAGEDAO_UPLOAD_ROOT` 成为首选变量名，旧的 `XIAKEDAO_UPLOAD_ROOT` 仍兼容
  - 外部 API 和表单仍然继续使用 `register`，内部字段改为避开框架冲突的名称
  - 上传链增加按 `upload_id` 删除运行态文件与索引的能力
- 定向验证：
  - `python -W error` 导入 `semantic_review/planning/workflow/independent_review` 相关模块
  - 结果：`register-import-check=ok`
  - `pytest tests/test_env_utils.py tests/test_runtime_paths.py tests/test_assembly.py tests/test_main.py tests/api/test_app_config.py tests/modules/test_upload_persistence.py tests/integration/test_upload_api.py tests/runtime_logging/test_sqlite_store.py tests/integration/test_health.py -q`
  - 结果：`46 passed`
  - 污染环境回归：
    - `LLM_PROVIDER=openai`
    - `LLM_MODEL=gpt-4`
    - 编排器 / workflow / opinion / semantic_review / tasks 相关测试仍通过 `102/102`
  - 前端哨兵：
    - `npm run build` 成功
    - `npm run test:run` 通过 `71/71`
- 本轮验证副产物清理：
  - `src/` 与 `tests/` 下 `__pycache__` 目录归零
  - `src/` 与 `tests/` 下 `.pyc/.pyo` 文件归零
  - `.pytest_cache/` 不存在
  - 仓库内 `data/tasks.db` 不存在
  - 外部 `../侠客岛-runtime/` 已清空并删除
- 本批结论：
  - 当前系统对“固定路径、固定端口、固定前端 dist、固定本机 `.env`”的依赖明显降低
  - `register` 相关框架告警已收敛
  - 运行态数据已经具备更明确的生成、恢复与删除边界

### 5.18 `docs/` 根目录第二轮归档执行

- 执行动作：
  - 新建归档目录：
    - `docs/archive/root-historical/`
  - 第一批迁出 `9` 个根目录历史文档：
    - `docs/内测发布说明.md`
    - `docs/侠客岛系统架构与工作流总览.html`
    - `docs/侠客岛最优建设方案_20260318.md`
    - `docs/侠客岛V2写作迁移提案_20260315.md`
    - `docs/一期建设方案.md`
    - `docs/night_bridge_real_validation.md`
    - `docs/三系统解耦_2C默认切换与最终解耦收口执行单_20260316.md`
    - `docs/三系统解耦_2C运行口径说明_20260316.md`
    - `docs/独立成稿模块改造长线程执行单_20260319.md`
  - 第二批迁出 `7` 个强历史关联文档：
    - `docs/V2知识库解耦方案_20260315.md`
    - `docs/藏经阁搬迁方案_20260315.md`
    - `docs/侠客岛搭建方案_20260315.md`
    - `docs/三系统解耦_2A全量搬迁与双消费验证执行单_20260315.md`
    - `docs/三系统解耦_2B-PDF提取与L3L4充实执行单_20260316.md`
    - `docs/三系统解耦长线程执行单_20260315.md`
    - `docs/三系统解耦基线施工汇报_20260315.md`
- 引用修正：
  - `docs/archive/root-historical/侠客岛长线程阶段拆分与审核要点_20260318.md` 已改指向 `docs/archive/root-historical/三系统解耦长线程执行单_20260315.md`
  - `docs/checkpoints/三系统解耦_2C-phase-3_checkpoint_20260316.md` 与 `docs/checkpoints/三系统解耦_2C-phase-5_checkpoint_20260316.md` 已改指向归档后的 `三系统解耦_2C运行口径说明_20260316.md`
  - `侠客岛-checkpoints归档迁出清单.md` 与本日志中涉及 `三系统解耦基线施工汇报_20260315.md`、`独立成稿模块改造长线程执行单_20260319.md` 的历史引用已改到归档路径
- 当前目录状态：
  - `docs/` 根目录文件数由 `28` 降到 `12`
  - `docs/archive/root-historical/` 当前共 `16` 个文件
  - `docs/checkpoints/` 保留 `41` 个文件，作为仍在项目记忆链中的 checkpoint 集合
  - 当前保留在 `docs/` 根目录的 `12` 个文件均属于总纲、设计图纸、阶段拆分、续接交接或当前入口文档
- 定向验证：
  - 对精确旧路径模式执行全仓搜索
  - 结果：活跃业务文档中的旧路径引用已收敛为 `0`
  - 剩余精确旧路径命中仅存在于本日志的迁出清单与 `docs/archive/checkpoints-unlinked/三系统解耦_2C-phase-6_checkpoint_20260316.md` 的历史记录中
  - 归档后目录复核结果：
    - `docs/` 总文件数 `92`
    - `docs/` 根目录文件数 `12`
    - `docs/archive/` 文件数 `39`
    - `docs/archive/root-historical/` 文件数 `16`
- 本批结论：
  - `docs` 根目录的历史执行单、历史方案和历史汇报已明显收缩
  - 当前仍保留在 `docs` 根目录的文档，更多是“当前入口与当前认知框架”，不再是无差别堆叠
  - 后续若还要继续瘦身，下一轮应改做“当前入口文档合并”而不是继续机械迁档

### 5.19 `docs/` 根目录第三轮归档执行

- 执行动作：
  - 再迁出 `7` 个根目录历史或失配文档到 `docs/archive/root-historical/`：
    - `docs/侠客岛SP-6核心去黑箱与正式交付目标与要求_20260319.md`
    - `docs/侠客岛_全模块主线建设总方案_20260317.md`
    - `docs/侠客岛_编辑部工作台与质量闭环执行单_20260316.md`
    - `docs/侠客岛完整写作系统建设规划.md`
    - `docs/侠客岛建设方案_严格事实版_20260318.md`
    - `docs/侠客岛长线程阶段拆分与审核要点_20260318.md`
    - `docs/长线程续接交接_20260318.md`
- 迁出依据：
  - `侠客岛_编辑部工作台与质量闭环执行单_20260316.md` 文档头已标记 `✅ 已收口`
  - `侠客岛SP-6核心去黑箱与正式交付目标与要求_20260319.md` 文档头已标记 `草案，待执行`
  - `侠客岛完整写作系统建设规划.md`、`侠客岛建设方案_严格事实版_20260318.md`、`侠客岛_全模块主线建设总方案_20260317.md` 仍将 planning / writing / quality / delivery / evidence 标成 `Seam-only`
  - 当前代码与前端入口已存在：
    - `src/api/routes/planning.py`
    - `src/api/routes/writing.py`
    - `src/api/routes/delivery.py`
    - `src/api/routes/evidence.py`
    - `src/api/routes/workflow.py`
    - `frontend/src/pages/StandardChainWorkspace.tsx`
    - `frontend/src/components/EvidenceQueryPanel.tsx`
    - `frontend/src/components/DeliveryPanel.tsx`
- 引用修正：
  - `docs/checkpoints/质量治理协作_stage-4_checkpoint_20260319.md`
  - `docs/checkpoints/统一终审_stage-5_checkpoint_20260319.md`
  - `docs/checkpoints/核心去黑箱_stage-6_plan_20260319.md`
  - `docs/checkpoints/主线长线程_最终收口报告_20260317.md`
  - `docs/checkpoints/独立成稿模块改造_stage-7_checkpoint_20260319.md`
  - `侠客岛-仓库清理审计报告.md`
  - 本日志中涉及旧根目录路径的条目
- 当前目录状态：
  - `docs/` 根目录文件数由 `12` 降到 `5`
  - `docs/archive/root-historical/` 当前共 `23` 个文件
  - 当前保留在 `docs/` 根目录的文件为：
    - `ADR-001-主编排器与模块边界.md`
    - `侠客岛介绍.md`
    - `侠客岛系统总纲评审记录_20260313.md`
    - `侠客岛系统设计图纸_20260313.md`
    - `侠客岛首阶段施工图纸_20260313.md`
- 定向验证：
  - `pytest tests/integration/test_direct_api.py::TestPlanningAPI::test_planning_plan_success tests/integration/test_direct_api.py::TestWritingAPI::test_writing_draft_success tests/integration/test_direct_api.py::TestDeliveryAPI::test_delivery_deliver_success tests/integration/test_standard_chain_api.py::TestStandardChainIntegration::test_evidence_planning_integration tests/integration/test_standard_chain_api.py::TestStandardChainIntegration::test_quality_delivery_integration -q`
  - 结果：`5 passed`
  - 对 7 个已迁文件旧根目录路径模式执行全仓搜索
  - 结果：活跃业务文档中的旧根目录路径引用已收敛为 `0`
  - 本轮验证副产物清理后：
    - `src/` 与 `tests/` 下 `__pycache__` 目录归零
    - `src/` 与 `tests/` 下 `.pyc/.pyo` 文件归零
    - 根 `.pytest_cache/` 不存在
- 本批结论：
  - `docs` 根目录现在只保留基础设计文档、评审记录和产品介绍
  - 已收口执行单、续接文档和与当前实现失配的旧方案已退出根目录入口层
  - 当前 `docs` 根目录已经更接近“给人看的当前框架”，而不是“混合着历史过程和旧状态判断”的堆栈

### 5.20 `docs/archive/` 历史开发文档删除执行

- 执行动作：
  - 删除 `42` 个已归档历史开发文档与孤立 checkpoint 附件
  - 删除前口径：只删除“已归档且无活跃非日志引用”的文件
  - 同步清空删空的归档目录：
    - `docs/archive/checkpoints-unlinked/`
    - `docs/archive/checkpoints-root/`
- 保留项：
  - `docs/archive/root-historical/三系统解耦_2C运行口径说明_20260316.md`
  - `docs/archive/root-historical/侠客岛SP-6核心去黑箱与正式交付目标与要求_20260319.md`
  - `docs/archive/root-historical/侠客岛_编辑部工作台与质量闭环执行单_20260316.md`
  - `docs/archive/root-historical/长线程续接交接_20260318.md`
  - 以上 `4` 个文件仍被 `docs/checkpoints/` 下的活动 checkpoint 直接引用，因此保留
- 删除规模：
  - 删除文件数：`42`
  - 删除体积：约 `588207` 字节，约 `574K`
  - 删除后 `docs/` 总文件数：`50`
  - 删除后 `docs/archive/` 总文件数：`4`
- 定向验证：
  - 归档删除后复核：
    - `docs/archive/root-historical/` 剩余 `4` 个文件
    - `docs/archive/checkpoints-unlinked/` 已不存在
    - `docs/archive/checkpoints-root/` 已不存在
  - 针对本轮已删除的 `42` 个路径逐项核对，结果：均不存在
- 本批结论：
  - 当前仓库中的历史开发文档已从“保留在 archive 里”进一步收缩到“仅保留仍承担回溯引用责任的最小集合”
  - `docs/archive/` 已不再堆积大批历史阶段文档，后续若继续清理，优先对象将转向根目录外的审计/执行报告，而不是开发过程文档

### 5.21 私有仓切换执行

- 执行动作：
  - 将原协作仓 `.git` 元数据完整移出当前工作区
  - 在当前目录重建一个新的本地 Git 仓库，作为后续测试与扩展的私有轨道
- 原协作仓信息：
  - 原分支：`master`
  - 原基线提交：`6650fda8f1c768048c3633c87822c7f6f3ddf79b`
  - 原远端：`origin = https://github.com/HuiduErxiang/XiaKe-Island.git`
  - 原 `.git` 备份路径：`/mnt/d/汇度编辑部1/.git-backups/xiakedao-collab-git-20260329-023804`
- 新私有仓口径：
  - 当前工作目录仍为：`/mnt/d/汇度编辑部1/侠客岛`
  - 新仓独立于原协作仓历史
  - 默认不继承任何 remote
  - 后续扩展、测试与提交都应在这套私有仓上进行
- 本批结论：
  - 当前工作区已经和协作仓远端脱开
  - 这轮清理和后续扩展不会自动暴露到原协作仓

## 6. 证据摘要

- 运行入口证据:
  - 进程内 `TestClient` 访问 `/health` 返回 `200`
  - 进程内 `TestClient` 访问 `/` 返回 `200`
- 前端证据:
  - `frontend` 下 `npm run build` 成功
  - `frontend` 下 `npm run test:run` 通过 `71/71`
- Batch 0 证据:
  - `.opencode/node_modules/` 删除前为 `739` 文件、约 `5.4M`；删除后路径不存在
  - `xiakedao.egg-info/` 删除前为 `5` 文件、约 `12K`；删除后路径不存在
  - 本轮验证副产物清理后，`__pycache__` 目录与 `.pyc/.pyo` 文件均归零
- Batch 3 证据:
  - `data/tasks.db` 删除前约 `2.9M`，含 `tasks=289`、`audit_events=111`
  - 删除后 `SQLiteTaskLogStore` 已重新建库建表，随后空库已再次删除
  - `src/data/uploads/` 删除前有 `3` 个上传目录、`6` 个文件、约 `7.1M`
  - 删除后 `UploadService` 已重建目录并写入探针文件，随后探针与空目录已删除
- Batch 4 证据:
  - 三份协作文档只命中自身与历史报告，不命中源码运行链
  - 删除后三个路径均不存在
  - 删除后前端 `build` 成功、前端测试 `71/71` 通过、后端 `/health` 与 `/` 均返回 `200`
- `checkpoints` 评估证据:
  - `docs/checkpoints/` 约 `332K`、`62` 个文件；`checkpoints/` 约 `216K`、`3` 个文件
  - `docs/checkpoints/` 中 `61` 个 markdown 里有 `40` 个存在文档入链，`21` 个无入链，零入链部分合计约 `60K`
  - `checkpoints/2026-03-17-empty-dict-serialization-fix.md` 仍被 `docs/checkpoints/编辑部工作台_batch-A-2_checkpoint.md` 引用
  - `checkpoints/2026-03-24-frontend-backend-unified-modification.md` 及其 `203K` 图片附件当前均无外部引用
- `checkpoints` 第一批迁出证据:
  - 4 个迁出目标已出现在 `docs/archive/checkpoints-root/` 与 `docs/archive/checkpoints-unlinked/`
  - 4 个原始路径均已不存在
  - 迁出后前端 `build` 成功、前端测试 `71/71` 通过、后端 `/health` 与 `/` 均返回 `200`
- `checkpoints` 第二批迁出证据:
  - 第二批 `19` 个零历史入链 markdown 已迁入 `docs/archive/checkpoints-unlinked/`
  - 迁出后 `docs/archive/checkpoints-unlinked/` 共 `21` 个文件，`docs/checkpoints/` 保留 `40` 个 markdown
  - 迁出后前端 `build` 成功、前端测试 `71/71` 通过、后端 `/health` 与 `/` 均返回 `200`
- 优化方案证据:
  - [src/api/app.py](/mnt/d/汇度编辑部1/侠客岛/src/api/app.py) 与 [src/runtime_logging/sqlite_store.py](/mnt/d/汇度编辑部1/侠客岛/src/runtime_logging/sqlite_store.py) 证明了此前任务库默认写回仓库，因而需要 `P0`
  - [src/modules/evidence/services/upload_service.py](/mnt/d/汇度编辑部1/侠客岛/src/modules/evidence/services/upload_service.py) 证明了 `P1` 之前上传文件可落盘，但上传索引仍在内存，因此需要补持久化边界
  - [tests/test_orchestrator.py](/mnt/d/汇度编辑部1/侠客岛/tests/test_orchestrator.py) 与 [src/modules/opinion/generator.py](/mnt/d/汇度编辑部1/侠客岛/src/modules/opinion/generator.py) 证明了 `P1` 之前 app-level 主链测试会受 provider 环境影响
- `P0 / 运行态出仓` 证据:
  - [src/runtime_paths.py](/mnt/d/汇度编辑部1/侠客岛/src/runtime_paths.py) 已统一 `XIAGEDAO_RUNTIME_ROOT`、`XIAGEDAO_DATA_DIR`、`XIAGEDAO_UPLOAD_ROOT`
  - [src/api/app.py](/mnt/d/汇度编辑部1/侠客岛/src/api/app.py) 已把共享 `TaskLogger` 改为懒初始化
  - `pytest tests/test_runtime_paths.py tests/runtime_logging/test_sqlite_store.py tests/test_main.py tests/integration/test_health.py -q` 结果为 `31 passed`
  - 默认路径探针显示：`/health` 后仓库内 `data/tasks.db` 仍不存在；`/v1/tasks` 后外部 `D:\汇度编辑部1\侠客岛-runtime\data\tasks.db` 被创建
  - 前端 `npm run build` 成功，`npm run test:run` 通过 `71/71`
- `P1 / 主链测试 hermetic 化` 证据:
  - [tests/conftest.py](/mnt/d/汇度编辑部1/侠客岛/tests/conftest.py) 已将 app-level 测试固定到 fake provider
  - `LLM_PROVIDER=openai LLM_MODEL=gpt-4 pytest tests/test_orchestrator.py tests/integration/test_workflow_api.py tests/integration/test_opinion_api.py tests/integration/test_semantic_review_api.py tests/api/test_tasks.py -q` 结果为 `102 passed`
  - `tests/test_provider_factory.py::TestCreateLLMProviderFromEnv::test_from_env_default_to_fake` 结果为 `1 passed`
- `P1 / 上传链持久化` 证据:
  - [src/modules/evidence/services/upload_manifest_store.py](/mnt/d/汇度编辑部1/侠客岛/src/modules/evidence/services/upload_manifest_store.py) 已将上传元数据固化为每目录 `manifest.json`
  - [src/modules/evidence/services/upload_service.py](/mnt/d/汇度编辑部1/侠客岛/src/modules/evidence/services/upload_service.py) 已在初始化时恢复上传状态索引
  - [src/api/routes/upload.py](/mnt/d/汇度编辑部1/侠客岛/src/api/routes/upload.py) 已新增单 upload 删除接口
  - `pytest tests/modules/test_upload_persistence.py tests/integration/test_upload_api.py -q` 结果为 `4 passed`
- `P2 / 入口解耦` 证据:
  - [src/main.py](/mnt/d/汇度编辑部1/侠客岛/src/main.py) 已支持 `XIAGEDAO_WEB_MODE=web-bundled/api-only`
  - [tests/test_main.py](/mnt/d/汇度编辑部1/侠客岛/tests/test_main.py) 已覆盖 bundled 首页与 `api-only` 根路径 `404`
  - `pytest tests/test_main.py tests/modules/test_upload_persistence.py tests/test_runtime_paths.py tests/runtime_logging/test_sqlite_store.py tests/integration/test_health.py -q` 结果为 `34 passed`
- `P3 / 路径与命名治理` 证据:
  - [src/env_utils.py](/mnt/d/汇度编辑部1/侠客岛/src/env_utils.py) 已统一 `.env` 文件装载、路径转换、布尔/CSV 环境变量解析
  - `python -W error` 导入结果为 `register-import-check=ok`
  - `pytest tests/test_env_utils.py tests/test_runtime_paths.py tests/test_assembly.py tests/test_main.py tests/api/test_app_config.py tests/modules/test_upload_persistence.py tests/integration/test_upload_api.py tests/runtime_logging/test_sqlite_store.py tests/integration/test_health.py -q` 结果为 `46 passed`
  - 前端 `npm run build` 成功，`npm run test:run` 通过 `71/71`
- `docs` 根目录第二轮归档证据:
  - `docs/` 根目录文件数已从 `28` 降到 `12`
  - `docs/archive/root-historical/` 已收纳 `16` 个根目录历史文档
  - 针对精确旧路径模式的全仓搜索显示，活跃业务文档中的旧路径引用已收敛为 `0`
- `docs` 根目录第三轮归档证据:
  - `docs/` 根目录文件数已从 `12` 降到 `5`
  - `docs/archive/root-historical/` 已收纳 `23` 个根目录历史文档
  - `pytest tests/integration/test_direct_api.py::TestPlanningAPI::test_planning_plan_success tests/integration/test_direct_api.py::TestWritingAPI::test_writing_draft_success tests/integration/test_direct_api.py::TestDeliveryAPI::test_delivery_deliver_success tests/integration/test_standard_chain_api.py::TestStandardChainIntegration::test_evidence_planning_integration tests/integration/test_standard_chain_api.py::TestStandardChainIntegration::test_quality_delivery_integration -q` 结果为 `5 passed`
  - 针对 7 个已迁文件旧根目录路径模式的全仓搜索显示，活跃业务文档中的旧根目录路径引用已收敛为 `0`
- `docs/archive/` 历史开发文档删除证据:
  - 删除前筛选口径为“已归档且无活跃非日志引用”，筛出 `42` 个删除目标
  - 删除后 `docs/` 总文件数为 `50`，`docs/archive/` 总文件数为 `4`
  - 删除后仅保留 `4` 个仍被活动 checkpoint 直接引用的 `root-historical` 文件
  - `docs/archive/checkpoints-unlinked/` 与 `docs/archive/checkpoints-root/` 已删除
- 私有仓切换证据:
  - 原协作仓 `.git` 已移至 `/mnt/d/汇度编辑部1/.git-backups/xiakedao-collab-git-20260329-023804`
  - 当前目录已重新执行 `git init -b main`
  - 原协作仓远端地址已从当前工作区剥离
- 当前收尾现场证据:
  - `find src tests -type d -name '__pycache__' | wc -l` 结果为 `0`
  - `find src tests -type f \\( -name '*.pyc' -o -name '*.pyo' \\) | wc -l` 结果为 `0`
  - 根 `.pytest_cache/` 不存在
  - 外部 `../侠客岛-runtime/` 不存在
- 历史后端测试基线证据（`P1` 之前）:
  - `pytest -x -q tests/test_main.py tests/test_assembly.py tests/test_orchestrator.py tests/runtime_logging/test_sqlite_store.py tests/integration/test_health.py`
  - 当时结果为 `1 failed, 8 passed`
  - 失败定位到 `tests/test_orchestrator.py::TestOrchestratorService::test_execute_opinion_module`
